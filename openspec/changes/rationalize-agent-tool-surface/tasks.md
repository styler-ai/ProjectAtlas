## 1. Shared surface contract

- [x] 1.1 Freeze the post-v0.5 CLI/MCP routing policy, capability specification, compatibility boundary, non-goals, dependency order, and valid evidence-led no-removal outcome.
- [ ] 1.2 Add the durable v0.6 agent-surface architecture views, render every Mermaid block with Mermaid CLI, and inspect visual communication plus semantic truth.
- [ ] 1.3 Map #310 as a direct native sub-issue of release-acceptance owner #493, prepare the exact sanitized issue-body mirror, reconcile its declared blockers, and pass the local IssueOps planned-issue gate.

## 2. Installed baseline and route decisions

- [ ] 2.1 Freeze one installed stable v0.5 runtime, canonical CLI, `atlas` CLI, MCP inventory and concrete schemas, serialized discovery bytes, plugin skill, generated hosts, platform matrix, and representative workflow baseline.
- [ ] 2.2 Run identical bounded CLI-first, MCP-first, and mixed evaluations for startup, orientation, search, exact retrieval, graph, health, purpose, token, administration, routing, freshness, recovery, and failure behavior.
- [ ] 2.3 Give every CLI route, MCP tool, alias, schema, skill reference, host expectation, and compatibility fixture one reviewed retained, merged, CLI-owned, automatic, or removed disposition without using absence alone as removal proof.
- [ ] 2.4 Ratify the smallest surface that preserves task success, freshness, explicit-root isolation, concrete schemas, typed errors, exact selectors, bounded output, and safe administration; retain a no-removal result when measurements do not justify change.

## 3. Public surface and compatibility implementation

- [ ] 3.1 Update the Rust CLI/MCP adapters at their existing shared service owners, retaining task status/cancellation only with a real packaged producer/cancellation lifecycle and adding no catch-all action multiplexer.
- [ ] 3.2 Update the version-matched ProjectAtlas skill so `atlas ...` owns ordinary one-shot shell work and MCP owns only the accepted capability-specific routes, with explicit concurrent project isolation and no stale names.
- [ ] 3.3 Update generated host configuration, installer/runtime/plugin convergence, typed inventory fixtures, CLI help and aliases, public documentation, migration notes, and rollback behavior as one versioned compatibility boundary.
- [ ] 3.4 Cover retained, moved, merged, removed, unknown-tool, wrong-root, missing-index, no-implicit-mutation, freshness, structured-output, administrative-confirmation, task, concurrent-client, migration, rollback, and supported-platform behavior in owning unit, integration, CLI/MCP, host, and E2E tests.
- [ ] 3.5 Run `cargo fmt --all -- --check`, `cargo check --workspace --all-targets --all-features --locked`, `cargo clippy --workspace --all-targets --all-features --locked -- -D warnings`, `cargo test --workspace --all-features --locked`, `cargo test --doc --all-features --locked`, and `RUSTDOCFLAGS="-D warnings" cargo doc --workspace --no-deps --all-features --locked`, plus ProjectAtlas lint, skill/runtime/plugin/host drift checks, and intended-scale route evaluation without weakening failures.

## 4. Installed v0.6 acceptance

- [ ] 4.1 Install the candidate on Windows, Linux, macOS x64, and macOS Apple Silicon and prove runtime, canonical CLI, `atlas`, MCP inventory, plugin skill, generated hosts, explicit roots, and version identity agree.
- [ ] 4.2 Run real stdio MCP, CLI, host-reader, concurrent-root, freshness, administration, migration, rollback, and representative agent-task E2E for every retained workflow and moved behavior with explicit timeouts.
- [ ] 4.3 Disposition authenticated review threads and automated findings, reconcile the final tool inventory and measured task outcomes, publish the compatibility note, and confirm #314 can depend on the accepted surface without duplicating authority.
- [ ] 4.4 Review the final implementation against the architecture diagrams, update the diagrams or implementation until they agree, or reconfirm the reasoned N/A.
