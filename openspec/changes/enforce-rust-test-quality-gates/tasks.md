## 1. Policy Baseline And Architecture

- [ ] 1.1 Add the single machine-readable test-quality policy with a schema version, exact `cargo-nextest` 0.9.140, `cargo-llvm-cov` 0.8.7, and `cargo-mutants` 27.1.0 pins, supported platform identities, applicable owned-Rust scope, and no blanket exclusions; unit test `TQG-UT-1.1` SHALL parse the complete policy and record a passing run before completion.
- [ ] 1.2 Record the measured 286-test, nine-suite, zero-ignored nextest inventory and 87.75% line, 84.90% region, 86.28% function, and 3,369-missed-line coverage snapshot with exact platform/commit/toolchain provenance and explicit non-completion wording; artifact unit test `TQG-UT-1.2` SHALL reject missing provenance or a false 100% claim and record a passing run before completion.
- [ ] 1.3 Record the measured 4,911-mutant snapshot and crate counts of 2,189 CLI, 951 DB, 587 service, 570 symbols, 441 core, 97 filesystem, and 76 lint candidates without making the snapshot an immutable current inventory; unit test `TQG-UT-1.3` SHALL reconcile the sum and require drift provenance before completion.
- [ ] 1.4 Commit explicit numeric v0.4 line, region, function, and viable mutation target agreements, distinguish floors from hard targets and raw from adjusted values, and link every unmet target to an open tracking issue without making it pass; policy unit test `TQG-UT-1.4` SHALL fail missing or unmet targets, every attempted target-gap waiver, and completion claims without counts before completion.
- [ ] 1.5 Declare bounded nextest, doctest, coverage, changed-mutation, full-inventory, shard, aggregation, and outer-job timeouts plus artifact retention that covers the release decision window; policy unit test `TQG-UT-1.5` SHALL reject absent, zero, unbounded, contradictory, or shorter-than-release retention values before completion.
- [ ] 1.6 Define exact coverage/mutation exception fields for source selector, category, rationale, owner, issue, approval, source identity, and future date/release expiry; unit test `TQG-UT-1.6` SHALL cover valid, expired, widened, overlapping, orphaned, and unused exception fixtures before completion.
- [ ] 1.7 Add an automated architecture-policy assertion that this change uses native configuration/workflow composition, adds no production crate or trait framework, preserves unconditional `unsafe_code = "forbid"`, and contains no Tarpaulin gate; artifact unit test `TQG-UT-1.7` SHALL pass and be recorded before completion.

## 2. Task Evidence And IssueOps

- [ ] 2.1 Extend the OpenSpec task parser/checker so every checkbox declares at least one unique stable task-specific unit-test identifier; unit test `TQG-UT-2.1` SHALL accept this checklist and reject missing, duplicate, malformed, and reused identifiers before completion.
- [ ] 2.2 Add the smallest machine-readable verification-plan and task-evidence schemas binding task/test identifier, escaped assertion, bounded executable/argument array, timeout, covered inputs, success status, tested implementation commit, normalized covered-input digest, relevant platform, timestamp, run ID/attempt/job/artifact identity/digest, and retained result; schema unit test `TQG-UT-2.2` SHALL round-trip valid data and reject shell strings, missing identity, caller-trusted URLs/status, and zero bounds before completion.
- [ ] 2.3 Validate that each required ledger row is successful, current for the tested implementation commit, covered-input digest, and assertion, non-orphaned, and backed by a retained result; permit only a metadata-only checkbox/evidence-pointer/GitHub closure commit with unchanged covered inputs, while unit test `TQG-UT-2.3` SHALL reject failed, stale, wrong-commit, changed-input, wrong-test, duplicate, and missing-result rows before completion.
- [ ] 2.4 Make local task completion fail when any declared unit-test identifier lacks current successful evidence and require planning/docs/policy tasks to name an automated artifact assertion; unit test `TQG-UT-2.4` plus integration fixtures SHALL reject premature source and documentation task completion before completion.
- [ ] 2.5 Extend `.github/scripts/issue-checklists.py` and `openspec/issue-map.json` handling to compare exact task text, order, one-owner ranges, test identifiers, evidence state, and checked state with authoritative GitHub checklists, reject missing/duplicate/extra/reordered tasks, and render idempotent non-checkbox per-section evidence comments from validated run identities; unit test `TQG-UT-2.5` plus mocked pagination/escaping/marker/concurrency fixtures SHALL detect each drift and renderer failure before completion.
- [ ] 2.6 Keep one primary issue authoritative when content fits and add deterministic disjoint phase-issue ranges only when generated content would exceed GitHub's 65,536-character body limit; unit test `TQG-UT-2.6` SHALL cover exact-boundary, over-limit, gap, overlap, duplicate-authority, and reordered-range cases before completion.
- [ ] 2.7 Wire task identifier/evidence/checklist validation into local check-in, issue status/closure, and release policy; run PR tests with read-only permissions, validate only explicitly linked authoritative issue/ranges and changed-artifact scope, retain full-milestone release completion, and add a separate trusted-default-branch `workflow_run` renderer with only narrow issue-write permission and no `pull_request_target`/PR-code execution; workflow-policy unit test `TQG-UT-2.7` and E2E failure/provenance smoke SHALL pass before completion.
- [ ] 2.8 Add compact self-test fixtures for valid single-issue/managed-comment and phase-ledger mappings, valid metadata-only closure, closure with covered-input drift, aggregate-green-but-task-evidence-missing, local/remote-only completion, orphan evidence, stale GitHub text, fork artifact, wrong run attempt/SHA/job/digest, duplicate marker, Markdown escaping, pagination, and retry/concurrency idempotency; fixture unit test `TQG-UT-2.8` SHALL execute every case before completion.

## 3. Typed Quality Validator

- [ ] 3.1 Add only the required test-quality validation command surface to the existing `projectatlas-lints` binary, reusing native tool exits and avoiding a new crate or orchestration framework; dispatch unit test `TQG-UT-3.1` SHALL cover help, valid subcommands, invalid arguments, and nonzero usage status before completion.
- [ ] 3.2 Model policy, platform, metric, exception, evidence, mutation outcome, shard, and validation errors with concrete structs and closed enums plus exhaustive matches and source-preserving errors; type/parse unit test `TQG-UT-3.2` SHALL cover every variant and unknown-value rejection before completion.
- [ ] 3.3 Enforce explicit repository-root isolation, canonical in-root policy/evidence paths, commit/lockfile binding, and read-only validation outside the designated evidence output; filesystem unit test `TQG-UT-3.3` SHALL cover wrong root, traversal, symlink escape, missing index, and no implicit ProjectAtlas mutation before completion.
- [ ] 3.4 Parse the native nextest, LLVM JSON, and cargo-mutants schemas needed for validation while retaining unknown-field compatibility and rejecting truncated or structurally incomplete evidence; parser unit test `TQG-UT-3.4` SHALL use pinned-tool success and corruption fixtures before completion.
- [ ] 3.5 Compute and verify `Cargo.lock`, policy, native-config, raw-artifact, command/profile, and repository identities in normalized manifests without trusting caller-supplied summaries; digest unit test `TQG-UT-3.5` SHALL detect one-byte changes and wrong-commit reuse before completion.
- [ ] 3.6 Implement exact exception matching and raw-versus-adjusted count reconciliation without deleting native rows or letting excluded/unviable outcomes improve raw metrics; reconciliation unit test `TQG-UT-3.6` SHALL cover exact, mismatched, expanded, and unused selectors before completion.
- [ ] 3.7 Compare proposed policy floors, targets, source scope, and exception reach against the trusted merge-base policy and fail every regression or unsupported increase; ratchet unit test `TQG-UT-3.7` SHALL cover line/region/function/mutation lowering, scope loss, and evidence-backed increases before completion.
- [ ] 3.8 Emit deterministic human and machine summaries with typed nonzero statuses for missing tool, no tests, no mutants, test failure, baseline failure, timeout, cancellation, corruption, incomplete output, and policy failure; status unit test `TQG-UT-3.8` plus CLI integration assertions SHALL pass before completion.

## 4. Nextest And Stable Doctests

- [ ] 4.1 Add repository-owned `.config/nextest.toml` with deterministic execution, explicit slow/test timeout behavior, JUnit output, and no retry-to-green policy; config unit test `TQG-UT-4.1` SHALL parse the native config and reject missing timeout/JUnit or permissive retry changes before completion.
- [ ] 4.2 Install and version-check pinned `cargo-nextest` in CI and release using the existing pinned action/installation style and fail before execution on mismatch; workflow unit test `TQG-UT-4.2` plus install smoke evidence on Linux, Windows, and macOS SHALL pass before completion.
- [ ] 4.3 Add an independent locked all-feature workspace nextest job with explicit timeout, complete suite/test/ignored/failed/timed-out inventory, and JUnit reconciliation; inventory unit test `TQG-UT-4.3` plus a successful real nextest integration run SHALL be recorded before completion.
- [ ] 4.4 Fail zero runnable tests and unexpected inventory shrink without treating test count as the quality score or preserving stale counts after reviewed changes; inventory unit test `TQG-UT-4.4` SHALL cover zero, shrink, approved update, and JUnit/native mismatch fixtures before completion.
- [ ] 4.5 Keep stable `cargo test --doc --workspace --all-features --locked` as a separate blocking job with its own timeout and evidence, without unstable LLVM doctest instrumentation; workflow unit test `TQG-UT-4.5` plus a real stable doctest integration run SHALL pass before completion.
- [ ] 4.6 Prove nextest success cannot mask doctest failure and doctest success cannot mask nextest failure using controlled fixtures and independent job conclusions; failure-propagation unit test `TQG-UT-4.6` plus workflow E2E smoke SHALL pass before completion.
- [ ] 4.7 Upload nextest JUnit and doctest logs/status with explicit retention and `if: always()` while preserving the originating job result; artifact-policy unit test `TQG-UT-4.7` plus a controlled failing-workflow smoke SHALL pass before completion.

## 5. Platform Coverage Ratchets

- [ ] 5.1 Add pinned `cargo-llvm-cov` plus matching `llvm-tools-preview` installation and a locked all-feature coverage command that emits LLVM JSON and a human-readable report without Tarpaulin or unstable doctest coverage; command-policy unit test `TQG-UT-5.1` plus tool-install smoke on every supported platform SHALL pass before completion.
- [ ] 5.2 Add independent coverage jobs for every supported release platform identity with explicit timeouts and separate line, region, function, and missed-count evidence; matrix unit test `TQG-UT-5.2` plus Linux, Windows, and macOS smoke runs SHALL prove no platform substitution before completion.
- [ ] 5.3 Parse LLVM counts and compute ratios from covered/total integers with deterministic rounding, per-file reconciliation, and no floating-point summary as pass/fail authority; metric unit test `TQG-UT-5.3` SHALL cover boundary, zero-total, rounding, overflow, and inconsistent-total fixtures before completion.
- [ ] 5.4 Apply the declared owned-production scope while retaining raw source rows and reporting every adjusted exception separately; scope unit test `TQG-UT-5.4` SHALL cover new production files, test fixtures, generated output, inline test code, platform-only code, and blanket exclusions before completion.
- [ ] 5.5 Establish Linux, Windows, and macOS baselines only from retained native runs on each platform, bind the measured 87.75/84.90/86.28 snapshot to its actual host, and reject copied placeholder floors; provenance unit test `TQG-UT-5.5` plus hosted baseline smoke evidence SHALL pass before completion.
- [ ] 5.6 Enforce monotonic platform-specific line, region, and function floors and require same-scope retained evidence before any increase; ratchet unit test `TQG-UT-5.6` SHALL cover one-dimension regression, platform pooling, unsupported increase, and valid increase before completion.
- [ ] 5.7 Enforce agreed numeric v0.4 targets independently from historical floors, block CI, review, and release whenever any agreed target is missed, treat tracking and expiry records as documentation only and never as waivers, and prohibit 100% wording without complete disclosed counts; target unit test `TQG-UT-5.7` SHALL inspect policy and generated summaries before completion.
- [ ] 5.8 Validate coverage exceptions, expiry, source identity, raw/adjusted reconciliation, and unused-record cleanup against real LLVM fixtures; exception unit test `TQG-UT-5.8` plus coverage integration fixtures SHALL pass before completion.
- [ ] 5.9 Upload LLVM JSON, human-readable coverage, normalized manifests, and diagnostics with `if: always()` while a controlled below-floor run remains failed; artifact-policy unit test `TQG-UT-5.9` plus workflow E2E smoke SHALL pass before completion.

## 6. Pull-Request Source Mutation

- [ ] 6.1 Resolve and retain the trusted pull-request merge base with sufficient checkout history and fail closed on shallow, missing, foreign, or ambiguous history; merge-base unit test `TQG-UT-6.1` plus temporary-repository integration fixtures SHALL pass before completion.
- [ ] 6.2 Add repository-owned base `.cargo/mutants.toml` with pinned deterministic settings, bounded build/test timeouts, all-feature workspace scope, native nextest test tool where supported, and no reviewed quality-policy exclusions; keep exact exceptions in typed policy and derive a retained filtered execution config/arguments only after raw listing; config unit test `TQG-UT-6.2` SHALL reject broad, unbounded, or raw-master-hiding exclusions before completion.
- [ ] 6.3 Use pinned `cargo-mutants` native diff selection against the recorded merge base and reconcile selected candidates with changed applicable production Rust; selection unit test `TQG-UT-6.3` plus add/modify/rename/delete integration fixtures SHALL pass before completion.
- [ ] 6.4 Require the unmutated baseline to pass before accepting changed-mutant outcomes and distinguish baseline, build, test, tool, and infrastructure failures; baseline unit test `TQG-UT-6.4` plus a controlled failing-baseline smoke SHALL pass before completion.
- [ ] 6.5 Parse every changed-mutant outcome and require zero viable misses, zero mutant timeouts, zero untested/unresolved candidates, and complete valid exclusions; disposition unit test `TQG-UT-6.5` SHALL cover caught, missed, timeout, unviable, excluded, and unresolved fixtures before completion.
- [ ] 6.6 Permit an empty selected inventory only when retained diff/scope evidence proves no eligible mutation exists, and fail tool, history, parsing, or truncation errors that look empty; empty-selection unit test `TQG-UT-6.6` plus non-Rust/Rust diff integration cases SHALL pass before completion.
- [ ] 6.7 Add an independent changed-source mutation CI job with explicit command/job timeouts, normalized manifest, annotations, native output, and `if: always()` artifacts; workflow unit test `TQG-UT-6.7` plus caught/missed/timeout E2E smokes SHALL pass before completion.

## 7. Full Mutation Shards And Ratchet

- [ ] 7.1 Generate one deterministic pinned unfiltered raw `cargo-mutants --list --json` master inventory with no reviewed policy exclusions for the exact commit/source-generator/base-config/tool identity and fail zero, truncated, duplicate, or malformed inventories; inventory unit test `TQG-UT-7.1` plus a real inventory smoke SHALL pass before completion.
- [ ] 7.2 Compare the current master with the measured 4,911/crate-count snapshot and require native source/tool/config-backed drift evidence rather than forcing the old count; drift unit test `TQG-UT-7.2` SHALL cover unchanged, explained, unexplained, and arithmetic-mismatch fixtures before completion.
- [ ] 7.3 Derive disjoint exact-excluded and filtered execution inventories from the raw master, then execute exactly 16 deterministic native `N/16` shards over the filtered identity with identical commit, policy, base/generated config, toolchain, feature set, and explicit command/job timeouts; shard-plan unit test `TQG-UT-7.3` plus one native shard smoke SHALL cover all shard numbers before completion.
- [ ] 7.4 Bind every shard manifest and native result to the master inventory and reject wrong commit, policy, config, tool, platform class, or inventory digest before aggregation; identity unit test `TQG-UT-7.4` SHALL cover each mismatch before completion.
- [ ] 7.5 Reconcile all shard candidate identities so executed union exact policy exclusions equals the raw master exactly once with no overlap, rejecting missing shards, gaps, duplicates, hidden exclusions, foreign candidates, cancellations, and incomplete outputs; reconciliation unit test `TQG-UT-7.5` SHALL exercise all 16 positions and every failure class before completion.
- [ ] 7.6 Classify caught, missed, timed-out, unviable, excluded, and unresolved outcomes separately with 100% raw-master disposition, report raw and adjusted metrics, and allow no raw-metric benefit from missing, excluded, unviable, or unresolved candidates; outcome unit test `TQG-UT-7.6` SHALL verify numerator/denominator rules before completion.
- [ ] 7.7 Establish the first viable kill-rate floor only from a complete retained 16-shard run and enforce a monotonic floor supported by same-policy evidence; ratchet unit test `TQG-UT-7.7` SHALL reject estimated, partial, lowered, wrong-policy, and regressed scores before completion.
- [ ] 7.8 Add scheduled, manual, and release-callable full-mutation workflow triggers without putting all 4,911 measured candidates serially on every pull request; trigger-policy unit test `TQG-UT-7.8` plus workflow-dispatch smoke SHALL pass before completion.
- [ ] 7.9 Aggregate and upload master inventory, 16 native shard outputs, manifests, normalized results, summaries, and failure diagnostics with explicit retention and `if: always()` while incomplete aggregates remain failed; artifact unit test `TQG-UT-7.9` plus missing-shard workflow smoke SHALL pass before completion.
- [ ] 7.10 Run the complete deterministic 16-shard baseline on the exact implementation commit, reconcile every current candidate, record the measured viable score without claiming near-100% unless proven, and commit the evidenced floor; baseline-report unit test `TQG-UT-7.10` plus all-shard integration evidence SHALL pass before completion.

## 8. CI, Release, And Required Checks

- [ ] 8.1 Restructure `.github/workflows/ci.yml` into independently named blocking nextest, doctest, platform-coverage, and changed-mutation conclusions while preserving strict verify/install/E2E jobs and replacing PR-wide milestone completion with linked-issue/range evidence validation; workflow unit test `TQG-UT-8.1` SHALL assert job names, dependencies, scoped PR behavior, full release behavior, and no masked dimension before completion.
- [ ] 8.2 Give every quality job and long command explicit policy-consistent timeouts and reject `continue-on-error`, permissive retries, ignored failures, or empty-success fallbacks; workflow unit test `TQG-UT-8.2` SHALL inspect all required jobs and controlled timeout E2E evidence before completion.
- [ ] 8.3 Bind release verification to passing independent evidence for the exact release commit, platform targets, changed-source mutation where applicable, and complete current 16-shard mutation before packaging or publication; release-policy unit test `TQG-UT-8.3` plus stale/missing evidence E2E fixtures SHALL pass before completion.
- [ ] 8.4 Preserve SHA-pinned GitHub actions, least permissions, read-only PR execution, trusted-default-branch/narrow-write IssueOps rendering, locked Cargo commands, warning-free Rust gates, and failure-safe artifact uploads; workflow-security unit test `TQG-UT-8.4` SHALL reject floating actions, `pull_request_target`, PR-code execution with write credentials, widened permissions, unlocked commands, caller-trusted run state, and success-masking uploads before completion.
- [ ] 8.5 Extend `projectatlas-cli` workflow-policy E2E coverage for exact tool pins, stable doctests, independent jobs, platform matrix, explicit timeouts, artifact retention/`if: always()`, 16 shards, release ordering, and no Tarpaulin; focused unit test `TQG-UT-8.5` plus the named E2E test SHALL pass before completion.
- [ ] 8.6 Add controlled workflow smoke fixtures proving nextest, doctest, each coverage dimension, changed miss/timeout, missing shard, stale evidence, and task-ledger drift each fail only the intended gate while diagnostics upload; smoke-harness unit test `TQG-UT-8.6` SHALL pass before completion.
- [ ] 8.7 Configure the final independent check names as required branch protections only after their evidence-producing workflows pass, and retain a GitHub API snapshot bound to the repository/ruleset; ruleset artifact unit test `TQG-UT-8.7` SHALL reject a missing or non-required gate before completion.

## 9. Local Workflow And Documentation

- [ ] 9.1 Update `.githooks/pre-push` to run pinned nextest, stable doctests, task-evidence/checklist validation, and existing strict Rust/ProjectAtlas gates with explicit local command timeouts where portable; hook-policy unit test `TQG-UT-9.1` plus a local hook smoke SHALL pass before completion.
- [ ] 9.2 Keep expensive platform coverage, changed-source mutation, and the full 16-shard run as explicit documented commands rather than silently running the complete mutation inventory on every push; scope unit test `TQG-UT-9.2` SHALL inspect hook and documentation behavior before completion.
- [ ] 9.3 Update `docs/workflow.md` with exact pinned install/run commands, evidence locations, failure meanings, platform floors/targets, exception workflow, and task-specific test ledger/checklist synchronization; documentation unit test `TQG-UT-9.3` SHALL validate every required command and section before completion.
- [ ] 9.4 Document the measured nextest, coverage, missed-line, and mutation inventory baselines with provenance, raw/adjusted distinction, and no 100% or no-bugs claim; claims unit test `TQG-UT-9.4` SHALL compare documentation to the policy and reject unsupported language before completion.
- [ ] 9.5 Add local-vs-hosted parity assertions for policy/config digests, exact commands, pins, timeouts, scope, and evidence schemas without requiring a ProjectAtlas index; parity unit test `TQG-UT-9.5` plus wrong-root/missing-index/no-mutation integration fixtures SHALL pass before completion.

## 10. Verification And Acceptance

- [ ] 10.1 Run all focused policy, validator, task-ledger, native-schema, exception, ratchet, inventory, and shard unit tests with explicit timeout and record each task-specific successful run; evidence-report unit test `TQG-UT-10.1` SHALL reject any task missing a current successful identifier before completion.
- [ ] 10.2 Run `cargo fmt --check` and record a clean result with no generated formatting drift; verification artifact unit test `TQG-UT-10.2` SHALL validate the command, commit, and zero status before completion.
- [ ] 10.3 Run `cargo check --workspace --all-targets --all-features --locked` with explicit timeout and record a clean result; verification artifact unit test `TQG-UT-10.3` SHALL validate the command, commit, and zero status before completion.
- [ ] 10.4 Run `cargo clippy --workspace --all-targets --all-features --locked -- -D warnings`, strict ProjectAtlas source lints, and strict rustdoc with explicit timeouts and zero warnings; verification artifact unit test `TQG-UT-10.4` SHALL validate every command/result before completion.
- [ ] 10.5 Run pinned all-feature nextest and stable workspace doctests with explicit timeouts, reconcile the current non-doctest inventory and JUnit, and record separate passing evidence; verification artifact unit test `TQG-UT-10.5` SHALL validate both independent manifests before completion.
- [ ] 10.6 Run pinned LLVM coverage on every supported release platform, retain raw reports, meet every committed line/region/function floor and hard agreed v0.4 target with no target-gap waiver, and record separate platform evidence; verification artifact unit test `TQG-UT-10.6` SHALL reject pooled, stale, waived, or below-agreement results before completion.
- [ ] 10.7 Run changed-source mutation success/failure smokes and the complete pinned 16-shard full mutation gate, reconcile 100% of the unfiltered raw master through executed plus exact-excluded candidates, and meet both the committed viable kill-rate floor and hard agreed target; verification artifact unit test `TQG-UT-10.7` SHALL validate all raw/filtered/shard/outcome evidence before completion.
- [ ] 10.8 Run `openspec validate enforce-rust-test-quality-gates --strict --no-interactive`, `openspec validate --all --strict --no-interactive`, task-ID/test-ID duplicate and gap audits, `git diff --check`, ProjectAtlas scan/lint, and IssueOps checklist/evidence synchronization with explicit timeouts; closeout artifact unit test `TQG-UT-10.8` SHALL validate every zero-status result before completion.
- [ ] 10.9 Run an independent Rust pattern-fit/architecture review proving concrete types, closed enums, typed errors, native tooling, no unnecessary crate/trait/generic/unsafe state, and unchanged product/MCP behavior; review artifact unit test `TQG-UT-10.9` SHALL require a disposition and verification link for every finding before completion.
- [ ] 10.10 Block v0.4 closeout until no known release-blocking defect remains, every agreed quality target has eligible passing evidence with no target-gap waiver, every OpenSpec/GitHub checkbox is synchronized, and every task-specific unit-test run is current; release-readiness unit test `TQG-UT-10.10` SHALL fail any missing, stale, waived, or unsupported acceptance claim before completion.
