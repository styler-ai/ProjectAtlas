## 1. Restore the planned-issue acceptance contract (#500)

- [x] 1.1 Freeze the audited planned-issue contract, eight-question Sol semantic review, mapped candidate task ownership, one tracked deterministic body-publication manifest containing the complete sanitized exact bodies, issue numbers, and SHA-256 values for issue #500 plus all twenty-nine accepted/current-candidate v0.5 issues, a separate non-authoritative promotion manifest for the twenty-nine-child future v0.5 release graph, release-readiness deltas, and focused architecture views; preserve authoritative task text and checked state, verify the ignored convenience body copies exactly match the tracked body manifest, and reconcile the packet with the completed primary Sol-owned personal global `issue-spec-writing` contract while the active release graph remains exact to current hosted state.
- [ ] 1.2 With Luna implementation ownership, implement the smallest objective IssueOps checker and self-test for the canonical section order, exactly one visible non-empty `Acceptance Criteria` section, two to five ordinary bullets, and rejection of checkboxes or OpenSpec task IDs; preserve every existing task, diagram, pre-mortem, graph, and publication gate, integrate the exact contract into applicable repository issue forms and repository guidance, verify compatibility with the primary Sol-owned personal global `issue-spec-writing` skill without mutating it, and update only the version-matched ProjectAtlas plugin guidance if its ownership review requires it.
- [ ] 1.3 With primary Sol-owned GitHub/specification mutation authority, after independent Sol review and a new hosted Codex review accept the corrected exact planning-PR head, validate the tracked body manifest's schema, unique exact thirty-issue set, normalized content, and SHA-256 values, publish only its reconstructed exact body bytes while that PR remains open and the authoritative/live graph remains at twenty-five children, and read back the same normalized bytes and hashes with mismatch or race fail-closed; keep the temporary body-to-`main` architecture-link gap fail-closed for readiness; rerun normal unfiltered IssueOps/CI to green; authorize/merge and read back the exact planning `main`; after accepted Luna task 1.2 implementation/readback, apply and read back the #500 milestone/status plus complete twenty-nine-child #492 native parent/blocker bootstrap from the accepted graph manifest; then, only after exact body, planning-main, and hosted relationship readback agree, use a separate narrow atomic PR to replace only authoritative `release_graphs.v0.5.0-00` with the exact #499 campaign declaration and future graph and remove both candidate manifests, requiring exact merged-`main`, live IssueOps, and fresh eight-question semantic readback while keeping all handoffs fail-closed on partial or raced state.
- [ ] 1.4 Review the final implementation against the architecture diagrams, update the diagrams or implementation until they agree, or reconfirm the reasoned N/A.
