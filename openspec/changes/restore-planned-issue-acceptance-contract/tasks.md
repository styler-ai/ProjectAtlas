## 1. Restore the planned-issue acceptance contract (#500)

- [x] 1.1 Freeze the audited planned-issue contract, eight-question Sol semantic review, mapped candidate task ownership plus a non-authoritative promotion manifest for the twenty-nine-child future v0.5 release graph, release-readiness deltas, focused architecture views, and exact sanitized migration drafts for issue #500 plus all twenty-nine accepted/current-candidate v0.5 issue bodies, preserving authoritative task text and checked state, and reconcile them with the completed primary Sol-owned personal global `issue-spec-writing` contract while the active release graph remains exact to current hosted state.
- [ ] 1.2 With Luna implementation ownership, implement the smallest objective IssueOps checker and self-test for the canonical section order, exactly one visible non-empty `Acceptance Criteria` section, two to five ordinary bullets, and rejection of checkboxes or OpenSpec task IDs; preserve every existing task, diagram, pre-mortem, graph, and publication gate, integrate the exact contract into applicable repository issue forms and repository guidance, verify compatibility with the primary Sol-owned personal global `issue-spec-writing` skill without mutating it, and update only the version-matched ProjectAtlas plugin guidance if its ownership review requires it.
- [ ] 1.3 With Sol-owned GitHub mutation authority, publish and read back the exact thirty-body migration; apply and read back the #500 milestone/status plus complete twenty-nine-child #492 native parent/blocker bootstrap from the accepted candidate manifest; promote the exact #499 campaign declaration and future graph into authoritative `release_graphs.v0.5.0-00` only after hosted state matches; then read back exact published `main` and perform the fresh eight-question semantic reconciliation for every accepted issue, keeping all handoffs fail-closed on partial or raced state.
- [ ] 1.4 Review the final implementation against the architecture diagrams, update the diagrams or implementation until they agree, or reconfirm the reasoned N/A.
