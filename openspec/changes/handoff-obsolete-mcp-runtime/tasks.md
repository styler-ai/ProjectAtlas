## 1. Installer Safety Boundary

- [x] 1.1 Make Codex plugin and registry skip flags control mutation only, with observed readiness required for managed convergence.
- [x] 1.2 Replace human-output substring matching with one exact JSON registry verifier shared by pre-update and post-update checks.
- [x] 1.3 Bind obsolete MCP retirement to one unambiguous process and final creation, path, complete command, version, and image identity.
- [x] 1.4 Preserve one bounded mirror retry and typed partial states without broad process termination.

## 2. Verification

- [x] 2.1 Cover exact registry acceptance plus substring, reordered, and extra-argument rejection.
- [x] 2.2 Cover skipped missing/stale plugin and registry readiness, process survival, ambiguous owners, and final identity mismatch survival.
- [x] 2.3 Run PowerShell parse/C# compile, `cargo fmt --check`, focused Clippy, installer trust-boundary tests, and affected installer E2E tests.

## 3. Contract and Communication

- [x] 3.1 Document the safe handoff, partial/retry states, and skip semantics in the host-integration guide.
- [x] 3.2 Update the focused installer architecture Mermaid view, render it with Mermaid CLI, and inspect the result visually.
- [x] 3.3 Map this OpenSpec change to GitHub issue 411 and synchronize its live checklist through IssueOps validation.

## 4. Release Gate

- [ ] 4.1 Verify the exact release version in a real installed Codex host: parent survives, obsolete child is replaced, and MCP initialization succeeds.
