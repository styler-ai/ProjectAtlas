## 1. Durable Delegation Contract

- [x] 1.1 Replace price-only or configurable-tier wording with the lowest reliable host-supported reasoning and cost rule, including fixed reliable bounded tiers without a reasoning selector and main-agent fallback only when isolated delegation is unavailable.
- [x] 1.2 Keep Codex Luna-low and Claude Code Haiku as conditional examples with a generic capability fallback so model names remain non-binding.

## 2. Runtime and Distributed Guidance

- [x] 2.1 Propagate the same rule through repository/distributed AGENTS guidance, packaged skill quick-reference/details, init, compact/expanded session brief, integration, navigation, and adoption surfaces without exceeding compact output bounds.
- [x] 2.2 Preserve bounded non-overlapping queue ownership, state tokens, immediate agent-approved purpose API writes, quiet maintenance, ProjectAtlas host non-spawning, and exact-root behavior.

## 3. Contract and Verification

- [x] 3.1 Synchronize the overlapping #305/#308 OpenSpec and mapped issue guidance, update and visually inspect the purpose-curator Mermaid view in light and dark themes, and keep documentation semantically aligned.
- [x] 3.2 Run cargo fmt --check, focused check/Clippy, owning unit and persistent stdio MCP tests, distributed-guidance/plugin/package contracts, issue checklist validation, hosted CI, automatic review, and land the completed v0.4.3 documentation change.
