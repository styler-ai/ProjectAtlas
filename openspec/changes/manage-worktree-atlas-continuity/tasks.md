## 1. Contract and Architecture

- [x] 1.1 Map `manage-worktree-atlas-continuity` to this GitHub issue in `openspec/issue-map.json` and keep the live checklist synchronized with this file.
- [ ] 1.2 Characterize ordinary single-checkout Git, primary/linked/bare-manager multi-worktree, separate clones/team identities, true non-Git, and Git-executable-unavailable modes plus relocation/recreation, nested cwd, branch/detached/dirty/unmerged state, existing databases, and optional seed/network availability without mutation.
- [ ] 1.3 Define and document local and portable repository/worktree identity, authored/derived/publication authority, exact-root generations, request-captured CLI/MCP routing, manager TUI selection, lifecycle states, failure semantics, migration compatibility, security/trust boundaries, and measurable architecture invalidation conditions.
- [ ] 1.4 Specify the deterministic purpose-delta encoding/provenance policy plus exact-tag GitHub release seed/manifest names, portable allowlist, self-reference-free source fingerprint, checksum/attestation, ignored cache/staging paths, retention, rollback, and size policy.
- [ ] 1.5 Freeze the seamless v0.4.4 upgrade contract for ordinary single checkouts and existing linked worktrees, including local-database precedence over seed hydration, plugin/skill/MCP repair, offline behavior, telemetry completeness, staged migration, retry, backup, and rollback boundaries.

## 2. Continuity, Seed, and Hydration Storage

- [ ] 2.1 Add the smallest repository-local SQLite continuity schema with stable keys, authority epochs, constraints, indexes, prepared/batched access, transaction/WAL ownership, backups, recovery, bounded event-admission/retirement state, and real migration round trips while keeping telemetry local and active derived atlases worktree-specific.
- [ ] 2.2 Add CI sealing and exact-tag stable/RC release publication that quiesces writers, checkpoints WAL, snapshots through the SQLite backup API or `VACUUM INTO`-style flow, keeps only the portable allowlist, resets host/root/session/process/task/telemetry state, excludes publication material from indexed input, verifies integrity/FKs/schema/runtime/parser/policy/config/source completeness plus real read-only CLI/MCP use, and emits checksum-covered content-addressed seed/manifest assets without changing RC Latest state.
- [ ] 2.3 Discover and verify the exact-runtime-tag compatible seed only when no valid active atlas exists, copy or safely reflink it without hardlinks into a staged ignored exact-root active database, rebind local identity, incrementally refresh additions/changes/removals and affected relations, atomically activate it under concurrent/crash-safe ownership, and fall back to ordinary local initialization when missing, offline, corrupt, tampered, stale, or incompatible.
- [ ] 2.4 Discover or create local continuity and durable worktree rebinding identity safely for primary, linked, bare-manager, relocated, recreated, copied, retired, separate-clone, and non-Git roots without requiring Git or confusing continuity/seed artifacts with a source atlas.

## 3. Purpose Promotion and Token Continuity

- [ ] 3.1 Project accepted folder/file purposes across compatible local worktrees only with exact reviewed content or explicit compatible target approval, keep local accepted responsibility durable across ordinary changes, retain changed inherited purposes as historical/review-needed, keep absent paths dormant, import unprovable legacy approvals as historical/unbound, and handle rename/delete/branch-only/ambiguous cases without leakage.
- [ ] 3.2 Export deterministic mergeable purpose promotions keyed by portable repository, normalized path, exact reviewed content identity, approval revision, and verifiable provenance; after each merge import only trusted final-main-compatible records, classify overlap/change/rename/delete/branch-only/rebase/retarget/stacked-base conflicts stale or inconclusive, reuse exact-content summaries/symbols, recompute affected final-main relations, and never merge contributor databases, WAL files, publications, or branch graphs.
- [ ] 3.3 Publish each CLI/MCP telemetry event exactly once through request-captured repository/worktree attribution, contiguous per-instance sequences, and compact high-water/closed-range state; safely evict raw detail and return exact lifetime/per-worktree/per-session aggregates under retries, gaps, concurrency, cancellation, crash/restart, sealing, and retirement while proving telemetry never enters Git, deltas, manifests, or seeds.

## 4. Worktree Control-Plane Surfaces

- [ ] 4.1 Preserve zero-ceremony exact-root init/scan/watch/CLI/MCP/TUI behavior for an ordinary single checkout and full local source/purpose/token/task/map/graph behavior for true non-Git and no-Git-executable roots; manager, seed, GitHub, and network discovery remain optional optimizations.
- [ ] 4.2 Let root selection establish one repository control root; automatically discover its structural Git/registered worktrees independent of folder names; admit additional exact paths only after reciprocal identity validation; and route them concurrently through one long-lived MCP server using authoritative per-call `project_path`, immutable worktree-config startup defaults, serialized session-default changes, and request-captured exact root/generation with CLI/MCP parity and no task, graph, telemetry, or write bleed.
- [ ] 4.3 Expose bounded lifecycle/seed status with exact path, branch/head/detached/dirty/merge evidence when available, atlas/runtime/schema/freshness, purpose/telemetry continuity, PID-reuse-safe process identity, completeness, blockers, and deterministic next actions across branch switch/rename/delete, older/diverged seed, relocation/recreation, retirement, and externally removed worktrees; automatically remove only proven-missing worktrees from active navigation while preserving bounded retired identity and continuity.
- [ ] 4.4 Expand the root token TUI into a complete bounded repository/worktree overview of lifetime totals, seed, active/retired registrations, Git evidence, atlas/runtime/schema/freshness, purposes, telemetry, processes, and blockers while rendering every source/graph map for only one visibly labeled selected exact worktree/generation.
- [ ] 4.5 Add dry-run-first ProjectAtlas retirement for existing or already-missing registrations that revalidates the exact target, treats PR merge as advisory evidence only, seals only its registration/contribution epoch, removes it from active selection without reducing lifetime totals, writes a bounded verified recovery manifest without copying rebuildable atlases, refuses uncertainty or live/unique state, never kills processes or mutates Git implicitly, and returns explicit Git-authority guidance.

## 5. Upgrade and Failure Recovery

- [ ] 5.1 Upgrade released v0.4.4 single-root and linked-worktree databases idempotently with source fingerprints and prepared/active receipts, engine-supported snapshots, active-WAL handling, exact event deduplication, typed incomplete/lower-bound handling for ambiguous aggregate history, held source exclusion, destination-verify-before-source-fence saga ordering, preserved backups, forward reconciliation, and typed refusal for malformed/newer/unfenceable schemas without blocking unaffected local navigation.
- [ ] 5.2 Recover deterministically at every seed staging/activation and continuity prepare/fence/registration-switch boundary plus concurrent hydration or pre-cutover CLI/MCP/watch writers, interrupted init/import/seal/retirement, process death/PID reuse/access denial, stale handles, branch switches/rebase/retarget, renamed/recreated/copied/missing worktrees, missing Git, offline hosts, corrupt/truncated/tampered state, and schema/policy/runtime drift without dual authority or lost/hidden accepted data.

## 6. Verification and Release Integration

- [ ] 6.1 Add owning unit and real SQLite integration tests for keys/constraints, accepted-purpose durability, promotion identity/provenance/conflicts, telemetry deduplication/aggregation, portable allowlist, query plans, writer quiescence, checkpoint/snapshot/read-only enforcement, migration fencing, every crash boundary, corruption, rollback, and forward recovery.
- [ ] 6.2 Add deterministic seed tests for clean-main sealing and repeated logical output, active WAL/writer refusal, digest/attestation tampering, truncation/private-state leakage, schema/runtime/parser/policy/config/source drift, exact-root copy/reflink activation, concurrent/crashed hydration, offline/missing seed fallback, and incremental equivalence with a clean full build.
- [ ] 6.3 Add real temporary-Git E2Es across primary and linked worktrees, bare managers, arbitrary and outside-root worktree paths, explicit registration, multiple clones/team identities, new files and accepted purposes, overlapping PR-like and stacked/sequential merge changes, branch switch/detached/rename/delete/rebase/retarget, older/diverged branches, dirty/unmerged source, external deletion, active-list retirement/recreation/relocation, nested cwd, missing Git, and safe fallback.
- [ ] 6.4 Add simultaneous-agent CLI/MCP/TUI E2Es proving one long-lived server plus root, client-worktree, cwd/config, and exact per-call selection never bleed defaults, roots, generations, graphs, tasks, purposes, telemetry, or writes; prove every advertised parity path and the TUI's complete overview plus single labeled selected-worktree map.
- [ ] 6.5 Add release-gate scenarios for real packaged v0.4.4-to-v0.4.5 upgrades of a SwipingGale-like ordinary checkout and an existing linked-worktree repository, including active WAL/process, interruption/retry, offline/Git-missing operation, unchanged full CLI/MCP/TUI behavior, local-database precedence over seed hydration, plus fresh multi-worktree seed seal/publish/hydrate/use on Windows, Linux, and macOS.
- [ ] 6.6 Profile representative large-source/many-worktree/high-event workloads for full seal versus incremental refresh, purpose import/relation closure, CPU, latency/startup, lock time, WAL/checkpoint and staging I/O, RSS, persistent/artifact/history bytes, concurrent-host fairness, and bounded output; establish enforced fallback and release limits.
- [ ] 6.7 Update the version-matched shipped skill, root `.gitignore`, user/upgrade/recovery documentation, architecture diagrams, exact-tag seed publication workflow, installer convergence checks, and release gates; render and visually review every changed Mermaid view and reconcile all live review feedback before task or issue closure.
