## 1. OpenSpec Task Checklist

- [x] 1.1 Bounded stderr followed by valid completion fails closed and destroys the resident session.
- [x] 1.2 Same-size/same-mtime launch-input mutation, including Linux native-import-policy drift, cannot retain authority before new resident launch or resident reuse: every digest read handle must match the previously captured file epoch, Unix change epochs detect drift, Windows guards prevent replacement, bounded currentness uncertainty terminates and reaps the resident, observed drift triggers bounded digest reload plus exact artifact-identity enforcement, and the caller's one pre-READY no-progress epoch is not reset by probe, reload, sealing, bounded process creation, admission, `SessionOpen`, or READY wait; a blocked spawn or stalled readiness/rendezvous cannot retain the caller past stop, and stop ordered before the successful final bounds check cannot transfer ownership while a later stop belongs to normal caller/resident cleanup, every untransferred child is killed and reaped by the process owner before the one launch lease is released, and late cleanup failure leaves later launches fail-closed.
- [x] 1.3 Native-import policy declarations above the worker's 1 MiB pre-containment ceiling are rejected before supervisor reads or retention; Linux residents execute and load only sealed verified worker/grammar bytes; Landlock denies every ABI v3-handled access to user-visible filesystem paths while the retained sealed anonymous grammar authority remains usable; executable objects work under `vm.memfd_noexec=1` with an `EINVAL`-only fallback, document memfds retain read-only non-executable mode on both modern and legacy creation paths, and the hosted lifecycle residue gate recognizes sealed workers by kernel-reported executable identity.
- [x] 1.4 External endpoint discovery executes bounded set-oriented indexed queries rather than two queries per affected entity.
- [ ] 1.5 Focused parser fault, cumulative no-progress, stalled-currentness, resident-cleanup, role-specific policy ceiling, Windows fixed-epoch sharing-guard, memfd-mode/fallback, sealed-worker residue, and repository-graph correctness/query-shape tests; changed Mermaid light/dark render plus semantic review; complete local release gates; exact-head hosted CI; and all live review threads pass.
- [ ] 1.6 #356 has no remaining implementation or proof work except the bounded task-state-only closure gates; PR #360 merge, issue closure, and promotion-head review are external transitions owned by #311 tasks 2.2 and 3.3.

Task 1.5 verification commands include `cargo test -p projectatlas-cli --lib --locked process_spawn -- --test-threads=1`, `cargo test -p projectatlas-cli --test e2e --locked default_scan_indexes_complete_accepted_core_surface -- --exact --nocapture`, `openspec validate --all --strict`, `python .github/scripts/issue-checklists.py --repo styler-ai/ProjectAtlas --root .`, the repository pre-push hook, and the exact-head hosted `01-CI` plus `optional-parser-pack` workflows. After tasks 1.5 and 1.6 are checked in the one allowed task-state-only commit and mirrored to issue #356, ordinary exact-head `01-CI`, strict OpenSpec, IssueOps, ProjectAtlas low lint, and fresh live-review checks rerun before merge and issue closure.
