## 0. Execution Order

Work in dependency order. Establish task 2.0's database architecture baseline before further functional implementation; at every later task transition, identify and complete the smallest required schema/key/index/query/transaction delta plus real SQLite write/read coverage before dependent Rust service or CLI/MCP behavior. Land significant compiling behavior slices into `dev` after focused behavior checks and ordinary locked Rust/workspace gates pass. Every non-trivial Rust slice and implementation/review subagent must assess intended-scale algorithmic, allocation/data-layout, filesystem-I/O, SQLite schema/index/query-plan/transaction/WAL, incremental-work, CPU/worker/concurrency, lock/backpressure/cancellation, memory/RSS, startup, persistent-size, and output performance; add a focused query-plan assertion, benchmark, or profile when inspection and behavior tests cannot establish the risk. Close each major implementation section with its explicit architecture-and-pre-mortem reconciliation task: trace live source and agent workflow through the applicable ownership, communication, publication, navigation, and failure views; classify each view as current or still-target; update, render, and visually plus semantically review Mermaid only when a durable boundary, arrow, invariant, failure transition, or public workflow changed; add, revise, or retire credible issue pre-mortem risks; map every mitigation to the smallest owning OpenSpec tasks; synchronize GitHub; and pass strict OpenSpec plus IssueOps. A no-change conclusion is valid only after this review and does not require artifact churn. A single coherent test may cover several related tasks. Do not add per-task tests, receipts, SHA ledgers, rendered evidence, issue sealing, or between-slice mutation/coverage machinery. Run one combined final changed-surface line-coverage and mutation audit only after the complete #308 behavior stabilizes at task 7.4.

## 1. Lean Planning And Source Recovery

- [x] 1.1 Replace the old evidence-driven #308 plan with lean proposal, design, capability specs, and behavior-slice tasks that preserve the accepted product scope.
- [x] 1.2 Map the change to issue #308, replace the stale GitHub status/checklist with this exact task list, and pass strict OpenSpec plus IssueOps validation.
- [x] 1.3 Review every retained old #308 branch and dirty worktree plus every functional requirement in the last complete 200-plus-task #308 specification; classify each old requirement as retained, improved, implemented, mapped to an open owning task, intentionally rejected with a durable product reason, or missing; add any missing product behavior to the smallest owning current spec/task, reject obsolete evidence/workflow machinery, and retire redundant branches/worktrees after the recovery decisions are recorded.
- [x] 1.4 Freeze representative 0.3.26 agent workflows and tasks for startup, purpose-led folder/file selection, inspect/summary, relations, and exact slice so navigation improvement can be evaluated without changing the normal funnel.

## 2. Typed Graph And Safe Storage

- [x] 2.0 Validate, document, and reconcile the ProjectAtlas database architecture against the future local-first workload before further graph mutation work: decide SQLite versus realistic embedded/server/graph alternatives; define engine/version/driver and local-filesystem/durability operating assumptions, authored/derived authority, conceptual/logical/physical entities, stable keys/constraints, purpose projection, hot query and index ownership, prepared/batched access, publication/read transaction boundaries, WAL/busy/checkpoint/statistics/concurrency behavior, migrations/backup/recovery, huge-source scale and resource limits, measurable engine-invalidation conditions, and the verification matrix; inspect the live schema, queries, publication, and recovery implementation and either correct every mismatch or assign it to the smallest owning unchecked task; add focused Mermaid authority, physical-model, publication, and bounded-read views, render and visually review them, and obtain independent Rust/database/SQLite architecture review without changing the accepted seven-crate boundary unless a real owner and consumer are proven.
- [x] 2.1 Add responsibility-owned typed graph identities, entities, logical relations, retained source occurrences, resolution, confidence, coverage, generation, reusable target selectors, and limits while preserving legacy relation compatibility and failing closed on stable-key collisions.
- [x] 2.2 Add read-only database/root/schema preflight and append-only migration ownership that preserves project identity, purposes, review state, settings, and telemetry on supported upgrades and refuses incompatible state without mutation.
- [x] 2.3 Implement validated bounded full/incremental staging that performs filesystem reads, hashing, parsing, graph extraction, and summary derivation before acquiring the main SQLite writer; revalidate source/configuration/base generation, then apply only prepared batched database mutation inside one short `BEGIN IMMEDIATE` publication transaction that keeps the last valid generation queryable and rolls back completely. Keep small admitted work in bounded typed Rust batches and select a bounded disposable working file, implemented as a plain file or SQLite-backed spool, only when measured cardinality, memory, recovery, and write-amplification behavior justify spill; even when SQLite-backed, the spool is not a ProjectAtlas database, has no project identity, authored state, active generation, or query surface, and is removed after publish/failure/restart. Do not restore physical slotting, copied live databases, or retained rollback generations unless a measured recovery/concurrency need justifies them.
- [x] 2.4 Implement one-transaction incremental structural deltas, with affected-row invalidation, one generation advance, and complete rollback on late failure.
- [x] 2.5 Persist and query typed graph, source-context, coverage, and lexical rows through indexed prepared operations that propagate corruption/iteration failures and avoid whole-graph scans or generic JSON hot paths.
- [x] 2.6 Implement explicit verified root-move and copy/detach behavior so independent worktrees or clones cannot collapse project identity and snapshots cannot overwrite destination identity.
- [x] 2.7 Map every existing and planned database-backed MCP/CLI call to its exact captured source-root/database/configuration binding plus bounded physical read/write, purpose-projection, freshness, generation, selector, trust/coverage, transaction, and output-byte contract; never auto-discover, attach, merge, substitute, fall back to, or combine another database inside one request; enforce the selected local-filesystem SQLite profile before writable WAL access, rejecting known unsupported or uncertain live network-filesystem state without mutation or a silent durability downgrade; audit and adapt the existing 0.4 storage tests so database behavior writes through real production transactions and reads or reopens through real production query/service/adapter paths. Use in-memory SQLite only where filesystem, WAL, migration, reopen, backup, busy/concurrency, and platform behavior cannot matter; use temporary on-disk production `AtlasStore` plus real CLI/MCP smoke otherwise. Mocked repositories, hand-built result rows, or SQL-text-only assertions do not prove persistence, query-plan, rollback, or agent-visible behavior.
- [x] 2.8 Bound the single project database and temporary-work lifecycle before graph volume grows further: replace the eternal implicit telemetry session with a bounded internal runtime/invocation instance distinct from an optional caller label; preserve exact session-scope `max(baseline without ProjectAtlas) - sum(emitted with ProjectAtlas)` deduplication while the instance is active, never silently reopen deleted baseline state after sealing/expiry, and keep global totals exact while session detail reports retained/partial/expired/unavailable. Retain recent raw usage telemetry under explicit row/age/logical-byte budgets, compact older measurements into bounded typed aggregates without changing supported token-report totals and declared trends, expose expired raw-detail and content-free retention/maintenance state honestly, remove only ownership-validated abandoned spill artifacts and obsolete derived state, and define measured WAL/checkpoint/statistics/free-page reuse or reclaim behavior without unbounded cleanup or blocking `VACUUM` on normal agent reads. Prove retention, aggregation, authored-state preservation, page reuse/reclaim, and real settings/token-report behavior with temporary on-disk production SQLite plus the real service/CLI/MCP paths; when a measured production spill owner exists, also prove ownership-validated crash/restart cleanup, otherwise prove an explicit no-spill/not-applicable state whose restart maintenance leaves arbitrary lookalike files untouched.

## 3. Freshness, Invalidation, And Bounded Work

- [x] 3.1 Make every normal index-backed read, watch, and one-shot refresh detect relevant local edits, deletes, renames, ignore/configuration changes, and optional VCS-state changes after edits or restart; reconcile a safe bounded delta or return typed `refresh_required` instead of serving known-stale file, summary, symbol, relation, or search results in git or non-git source trees.
- [x] 3.2 Preserve last-valid facts on transient path/root/database/permission uncertainty and re-resolve inbound dependents when exported identities or dependency keys change; persist typed indexed canonical export/resolution keys and relation-occurrence dependency keys qualified by every identity-affecting project, provider/language, package, scope, and relation dimension so the union of prior and newly staged keys finds resolved, ambiguous, and previously unresolved inbound sources without display-name scans; let SQLite return bounded indexed candidate sets, then let Rust union keys, deduplicate owning sources, apply resolver semantics, admit or escalate the affected closure, and construct the prepared batch for one atomic SQLite publication. Cover add/rename/delete/duplicate/made-unique resolution transitions and preserve the prior complete generation when aggregate key/path/row/byte/time/cancellation limits require typed full-refresh escalation. Preserve the 0.3.26 `generated + suggested` versus `agent + approved` purpose contract, remove automatic approved-purpose invalidation on source/hash/graph changes, normalize legacy purpose-`stale` rows safely during the already-unmerged schema migration, keep absent-path purposes dormant, do not transfer approval automatically on rename, and keep explicit agent/user correction through existing purpose APIs.
- [x] 3.3 Prove create, modify, move, rename, delete, ignore/unignore, parser/configuration change, interruption, and unchanged-dirty sequences converge to clean-scan graph, coverage, and lexical results without repeated no-change publication.
- [x] 3.4 Keep exact-byte indexing and freshness verification cancellable and resource-bounded, preserve last-valid queries during failed work, leave failed or canceled watcher batches eligible for retry, and allow isolated projects to progress without one process-global lock; when isolated workers/packs exist, derive an effective resource envelope from configured safe limits plus supported host/container/job constraints.
- [x] 3.5 Prevent freshness from defeating bounded agent queries: in a long-lived MCP/watch runtime, activate relevant root/policy observation before or during the first exact post-start verification, reconcile buffered events, and establish a process-scoped verified source-observation epoch; bind database snapshots, results, and cursors to that epoch plus active generation, reuse it for later unchanged calls without another whole-tree walk or full node-table load, and invalidate it on relevant events, observer overflow/gap/disconnect, root/policy uncertainty, cancellation, or a source change during the query. Reconcile, retry, exactly re-verify, or return typed `refresh_required` before claiming current facts; keep one-shot CLI first verification honest. Prove restart after offline dirty/non-Git changes, watcher continuity failure, mid-query edits, cancellation, and repeated unchanged folder/file/summary/relation calls, measuring filesystem entries/bytes, SQLite statements/decoded nodes, latency, and output so the second and later healthy-epoch calls remain proportional to their bounded query rather than repository size.
- [x] 3.6 Close the freshness and incremental-integrity section with the required architecture-and-pre-mortem reconciliation; in particular verify the source-observation epoch/invalidation, dependency-aware staged publication, SQLite snapshot/generation ownership, cancellation/retry/failure, and one-shot-versus-long-lived flows against live source and measured behavior before language capability work proceeds.

## 4. Language Capability And Resolution

- [x] 4.1 Generate deterministic detection, parser ownership, fixtures, tiers, settings, and documentation inputs from one versioned Rust language capability registry and accepted capability-set manifest; make the complete 0.3.26 detected/native/manifest/structural/fallback surface the compatibility floor, admit broader detection-only or honestly labeled fallback rows without inflating parsed/symbol/semantic/benchmarked claims, derive every count, and reject silent loss, weakening, ghost rows, aliases presented as capabilities, or unbound parser owners.
- [x] 4.2 Preserve every current built-in language, exact filename, compound extension, case behavior, parser selection, parse metadata, structural summary, fallback visibility, and empty-native-parse result while adding typed deterministic explicit-override, exact-filename, compound-extension, extension, and bounded content/dialect precedence with a compatibility corpus and testable reason codes.
- [x] 4.3.1 Select and exactly pin one broad-grammar source plus one ProjectAtlas-owned consuming runtime while preserving seven crates; freeze one generated accepted-capability manifest with per-language grammar provenance, subtree license text, ABI/export identity, digest, a natural positive fixture, an explicit upstream-error or deterministic incomplete editor-state negative fixture derived from exact natural upstream source with retained transformation provenance, platform applicability, and built-in-owner precedence. Task 4.3.2 must prove every accepted negative through the selected grammar on every accepted optional-pack target before it contributes to a support claim. Treat an upstream catalog size only as input inventory, never as a ProjectAtlas support claim.
- [ ] 4.3.2 Build or repackage one logical optional pack into self-contained artifacts for the accepted optional-pack target set—Linux x86-64 and Windows x86-64 in v0.4.0; prove network-disabled offline construction after dependency fetch, individual file digests, forbidden-import policy, clean-machine loading, package/install bounds, and at least 150 distinct additional non-built-in grammar-backed rows that satisfy the same accepted manifest on both targets. Keep macOS ProjectAtlas and the complete 0.3.26 built-in parser surface supported, but ship no macOS optional-pack artifact and return typed `unsupported_containment` for optional-pack activation there. Split artifacts only if measured package/install/platform limits require it, never to create a generic multi-pack framework.
- [ ] 4.3.2.1 Select and prototype the exact fail-closed containment launcher before loading an untrusted grammar on each accepted optional-pack target: clear and allowlist environment plus inherited handles; allow read-only immutable pack manifests/libraries and only the exact unavoidable loader/runtime state admitted below, with no repository path or other filesystem reach; deny child creation, further exec, shells, compilers/builds, DNS/direct sockets/network, and breakaway. On Windows, ship one artifact-bound responsibility-specific x86-64 PE32+ containment broker as the Rust supervisor's direct child; bind its explicit Windows .NET Framework CLR v4 runtime, ordinary PE-loader imports, and reflected managed P/Invoke module/method digest into the immutable artifact audit; it owns exactly one suspended AppContainer/LPAC parser-worker grandchild and one no-breakaway Job Object, passes only the supervisor-created stdin/stdout/stderr endpoints through an exact `HANDLE_LIST`, configures zero capabilities, child-process denial, active-process limit one, per-process/job committed-memory ceilings, and kill-on-close, verifies admission, resumes, and emits one fixed bounded adapter-local admission record before Rust sends `SessionOpen`. Treat the empty artifact-scoped AppContainer profile as bounded unavoidable Windows sandbox state, expose no user/repository data through it, and remove it through the pack lifecycle. On Linux, build and audit the trusted worker so the exact allowed system runtime DSOs are eager `DT_NEEDED` mappings before `main`, start it without grammar or source input, require exact protocol pipes plus one thread, then install hard resource/address-space limits, `no_new_privs`, hard-require fully enforced Landlock ABI v3 with read-only access only to the immutable pack root, and install seccomp process/exec/socket denial before reading the one bounded `SessionOpen`. The opening carries only a fresh supervisor-generated session identity; READY must bind that session to the exact observed artifact and admitted containment kind, and the supervisor sends no grammar identity or source bytes until READY validates. Add delegated cgroup-v2 accounting when available without making ordinary-user delegation a hidden installation prerequisite. Prove successful confinement, fail-closed admission, exact artifact/update compatibility, declared observation interval/maximum overshoot for any sampled resident-memory kill path, and cleanup on both targets. Prove that macOS activation returns typed `unsupported_containment` before worker launch/source transfer while built-in parsing remains available. Do not substitute protocol omissions, post-load restrictions, `sandbox-exec`, cgroups, or Job Objects alone for a capability boundary.
- [x] 4.3.2.2 Separate file-level source parse provenance from fact-graph provenance in the smallest compatible schema/API delta before optional parsing enters runtime staging: migrate existing parser metadata without weakening it, reconstruct `SymbolGraph.parser` and normalized relation confidence/coverage from fact provenance rather than source-parse success, preserve per-symbol/per-relation provenance, and prove write, commit, reopen, reload, incremental reprojection, empty-fact graphs, and conservative coverage for `source parser = tree-sitter` with fallback symbols/relations.
- [ ] 4.3.2.3 Prove Windows optional-pack construction and containment recovery through one bounded elevated hosted suite using the production adapters: abruptly exit a nested construction wrapper after the exact disposable account is created but before its SID replaces the durable placeholder journal, then recover only the validated generated name, description, and SID; inject construction-launcher and shipped-broker failure after suspended child creation but before Job assignment, terminate and bounded-wait the exact PID, and prove it absent; start an exact-SID long-lived profile-backed child, make the first cleanup attempt fail after process termination but before profile/state completion, retain the protected journal, then succeed once and prove a third cleanup is an immediate no-op. Independently verify zero exact account, profile, process, firewall, ACL, state, and handle survivors; preserve both operation and cleanup failures; run one disposable identity at a time under fixed deadlines; and keep every workflow fallback cleanup unconditional.
- [ ] 4.3.3 Implement the separately shipped grammar-affined ProjectAtlas parser supervisor/worker protocol with raw bounded source bytes and no repository path, shell, compiler, build, environment, URL, or network surface. Keep one concrete process-owning supervisor in `projectatlas-cli` and reuse it unchanged from normal runtime staging and a CLI-owned fresh-artifact verifier; keep only artifact/protocol facts and validation in `projectatlas-core`, and remove the direct-worker/core-example verification path rather than maintaining a second weaker launcher. Use one global broker-owned optional-pack worker session per ProjectAtlas process for v0.4.0—Linux launches the worker directly in its owned process group, while Windows launches the artifact-bound containment broker that owns exactly one worker—one grammar at a time, deterministic grammar/path job grouping, and bounded restart on grammar change or unhealthy state. Keep fixed-before-allocation framing; Windows Rust validates the broker's fixed admission record first; one strict pre-READY `SessionOpen` containing only protocol version plus fresh supervisor entropy; READY bound to that exact session, the independently observed artifact, and the admitted containment kind; request/progress/completion/failure replay rejection across sessions; session-bound monotonically sequenced progress that never extends the absolute deadline; input/frame/output/stderr/time/no-progress/aggregate-memory/concurrency budgets; existing task-owned cancellation/admission; exactly-once termination, pipe draining, reap/join verification; and typed pre-publication failure control in bounded Rust. Keep environment/handle/filesystem/process/exec/network/memory admission and process-group/job kill in the proven Linux/Windows adapters from 4.3.2.1, with typed `unsupported_containment` before launch on other hosts; on Windows Rust owns protocol/deadline/cancellation and direct-broker reap while the broker owns exact worker/job cleanup and succeeds only after it has waited and cleaned up; keep grammar-backed source parse metadata distinct from conservative fallback symbol/relation fact provenance through the 4.3.2.2 round trip; and preserve background task/status/cancel/read responsiveness plus the previous active generation after every crash, forged/replayed/stalled admission, READY, or progress sequence, blocked write, timeout, malformed result, flood, limit, cancellation, containment, or cleanup failure without adding another task registry or executor.
- [ ] 4.3.4 Implement explicit verified install, verify, enable, update-with-atomic-rollback, disable, and remove operations for the optional pack. On unsupported hosts, verify, install, enable, update, and invoke return typed `unsupported_containment` before artifact acquisition, worker launch, source transfer, lifecycle-state mutation, or accepted-surface weakening; disable and remove remain safe and idempotent for stale local metadata. Keep normal use offline and prove default core has no optional-pack download, compilation, linkage, initialization, binary, startup, or idle-RSS burden and no accepted-surface weakening when the pack is absent.
- [ ] 4.3.5 Close the optional-pack boundary by reconciling the registry, settings, generated capability/provenance/license documentation, architecture drawings, pre-mortem, accepted-platform matrix, typed unsupported-containment behavior, clean-machine evidence, and package/startup/RSS measurements; do not advertise optional grammar breadth until every accepted row and accepted optional-pack target passes.
- [x] 4.4 Add normalized project-wide registries and independently structured semantic providers with resolved, ambiguous, unresolved, and external outcomes plus non-vacuous positive and negative fixtures for every advertised family; accept typed compiler metadata only as bounded non-executable, secret-redacted data where required; include bounded host-to-embedded byte/line/span reconciliation for accepted HTML-like, component, and template hosts.
- [x] 4.5 Make existing settings report schema/migration compatibility, active generation, optional selected-slot detail, registry/provider/relation digests, actionable coverage, lexical/FTS/search state, optional-pack lifecycle, and the validated content-free SQLite operating profile—runtime version/compile-feature identity, supported/unsupported/uncertain filesystem profile, journal/synchronous/busy/checkpoint/statistics policy—from validated state without secrets or new machine-local paths; derive capability matrices, provenance/license inventory, and public support claims from the same authority.
- [ ] 4.5.1 Define and implement one versioned fixed complete-support profile schema plus its candidate-profile and documentation-only ecosystem catalog projections, then obtain independent Rust/language-capability review. Define closed `language`, `dialect`, `domain_format`, and `framework_projection` kinds separately from presentation categories/tags; require kind-owned mandatory, machine-checkable evidence slots bound to registry/parser/provider/relation/publication/navigation identities; permit `not_applicable` only through a typed schema-admitted reviewed reason; bind every framework to an exact host profile; require independent dialect evidence beyond a shared extension; keep planned/unavailable assessments out of runtime capability rows and every capability total; and keep every candidate at its achieved lower tier until task 7.3 revalidates the final MCP navigation surface. Extend and test the existing generated `docs/language-support.md` authority, derive a grouped `Language & Ecosystem Support` HTML Pages projection from the same catalog identity, add and test repository/site landing links, explain the detection-to-navigation pipeline and ProjectAtlas advantages, retain canonical Mermaid source in GitHub-rendered Markdown and either embed a reviewed SVG or link to it from Pages, and link the system/component, crate-ownership, database-authority, graph-physical-model, bounded-read, MCP-read, and publication views. Include a legacy-modernization tag without claiming automatic conversion or a target language. Explicitly classify—not silently conflate—ABAP versus ABL; COBOL and Fortran dialect/extensions; PL/I, RPG/ILE RPG, JCL, REXX, IBM i CL, HLASM and other assembler families, Ada, Pascal/Object Pascal/Delphi, Visual Basic 6, VB.NET, VBA, VBScript and Classic ASP, SAS, Natural, MUMPS/M, ABL, PowerBuilder/PowerScript, xBase/Clipper/FoxPro, Perl, ColdFusion/CFML, and ActionScript/Flex; VHDL, Verilog, and SystemVerilog under a separate hardware-design category; SQL, Oracle PL/SQL, PL/pgSQL, T-SQL, MySQL/MariaDB, SQLite, BigQuery, Snowflake, Redshift, and dbt/Jinja SQL; Terraform, OpenTofu, HCL, Bicep/ARM, CloudFormation/SAM, Pulumi, Kubernetes/Helm/Kustomize/Crossplane, Ansible, Nix, CUE, Dockerfile/Compose, and host-language CDK constructs; and Playwright plus other accepted test-framework projections bound to exact hosts. These named families are explicit classification obligations, not automatic v0.4.0 complete-support commitments. Reuse maintained license-compatible Tree-sitter grammars, generated parser/node-type metadata, and trustworthy standard query assets first; add bounded ProjectAtlas queries and concrete Rust semantic providers only for missing facts and resolution; create or fork a grammar only as a documented last resort, and do not prebuild a generic extraction DSL, runtime provider framework, or another parser pack.
- [ ] 4.6 Close the language-capability and resolution section with the required architecture-and-pre-mortem reconciliation; verify the complete 0.3.26 compatibility floor, registry ownership, detection/parser/provider precedence, accepted capability and relation truth, embedded-span reconciliation, settings derivation, incomplete/ambiguous coverage, honest breadth totals, the independently reviewed fixed complete-support schema, candidate profiles, generated grouped catalog projections, and optional-pack/core-independence boundaries against live source, non-vacuous fixtures, and absence/failure measurements before graph context enters normal navigation.

## 5. Agent-First Navigation And Analysis

- [ ] 5.1 Extend the purpose queue with coalesced task/generation/path-scoped missing/suggested rows and expose one actionable bounded purpose-curator handoff through the existing init, session-brief, and queue surfaces without adding a trigger tool. Update packaged host-neutral guidance/agent profiles to automatically delegate each actionable `low`-scope batch to an isolated purpose-curator subagent at the lowest reasoning tier the host can enforce; when the host cannot enforce subagent execution or reasoning selection, allow the main agent to process the same bounded batch instead. Bind deterministic batch/item work keys and conditional-apply state tokens to the selected project, active generation, task, path, and current unapproved row so concurrent or stale curators cannot overwrite an accepted or changed purpose; keep explicit accepted-purpose correction as a separate deliberate path. Preserve generated suggestions as unapproved until an agent accepts them, skip accepted purposes unless an explicit inconsistency/correction task is supplied, keep successful maintenance out of normal responses, never start `medium`/`strict` implicitly, never claim the Rust server spawned an agent, and never write SQLite directly.
- [ ] 5.2 Enrich existing folder and file rows before summary with reviewed one-line purposes plus crisp bounded package/import/call/reference/test/route/config connections, while preserving exact path/name and strong purpose priority with deterministic compact reason codes.
- [ ] 5.3 Add deeper bounded relationship/coverage digests, opt-in project-wide coverage discovery with path, parser/provider, relation, state, and reason filters plus complete/partial/failed/ignored/oversized/quarantined/stale outcomes, and typed next-call hints to selected-file summaries/health surfaces so agents gain useful context without default edge dumps, extra mandatory calls, or an implicit scan.
- [ ] 5.4 Preserve deterministic lexical search semantics; for indexable literal/token shapes use FTS/BM25 only as a complete candidate superset followed by exact verification, while arbitrary regex/fuzzy/short/punctuation-sensitive or tokenizer/Unicode-unsafe shapes use the correctness-authoritative path/byte/time-bounded persisted-text fallback with searched-file/searched-byte/truncation state and measured work proportional to the selected text scope. Add explicit optional `semantic`/`hybrid` modes with typed unavailable-state errors, lexical-complete hybrid ranking, and differential fallback-equivalence coverage.
- [ ] 5.5 Extend existing relation queries additively with typed direction, extended family, depth, confidence, resolution, retained source occurrences, reusable exact target selectors/next calls, pagination, and hard limits while old requests retain legacy rows and ordering; an agent already anchored at a file or symbol must traverse ranked inbound/outbound connections directly without repeating overview/folder/file discovery. Keep entity lookup, one-hop inbound/outbound and family pages, exact occurrences, coverage, and unique endpoint/accepted-purpose hydration in bounded prepared SQLite operations under one request snapshot; keep multi-hop frontier control, node-simple visited/cycle handling, trust/relation filtering, ranking/path selection, cancellation, aggregate budgets, truncation reasons, and final result composition in `projectatlas-service`. Expand Rust frontiers through bounded direction-specific batched SQLite adjacency queries, never one query per node, a source-or-target predicate that defeats owning indexes, or a full-graph materialization. Every returned local-source node or path step must project its authoritative accepted owning file/folder purpose plus approval/provenance state without duplicating purpose ownership into derived graph rows, while external or unresolved nodes report purpose as not applicable or unavailable. Update the packaged v0.4 ProjectAtlas skill with a concise task-to-tool routing table that distinguishes startup and ranked candidates, one-file intelligence, declaration lookup, inbound/outbound relation or bounded graph projection, indexed text discovery, and exact source evidence; make `atlas_symbol_relations` the explicit relation route and state the next call for each result shape. Measure a separately indexed recursive CTE against the bounded Rust frontier as a performance guard and keep the mechanism that representative cyclic/high-degree measurements show best preserves plans, topology, cancellation, trust, deterministic ranking, and visited/edge/time/memory/output accounting; Rust remains the expected result, not an unmeasured foregone conclusion.
- [ ] 5.6 Add bounded architecture, relationship-derived component/community candidate, dependency-cycle/SCC candidate, reviewed-purpose alignment/drift, language-valid complexity/bottleneck candidate, VCS-aware impact/dead-code candidate, and node-simple static trace views through existing summary/health/relation services; `trace` means bounded static relationship/path inspection over indexed source facts, not runtime execution-trace ingestion. Keep inferred architecture candidate-labeled, deterministic, selector-bearing, coverage-aware, opt-in when expensive, and explicitly truncated under row/edge/time/output budgets; add at most one optional closed analysis tool only if real agent tasks prove the relation request becomes less clear, and do not add a generic query language or fourth complexity tool. Shared behavior coverage must include a deterministic cross-folder community, matching and conflicting purpose cases, one real cycle plus an acyclic negative fixture, reusable source selectors, visible ambiguous/incomplete coverage, and explicit budget truncation.
- [ ] 5.7 Bind cursors to project/root, active generation, authored-purpose revision when purpose affects membership/ranking/hydration, capability, query, filters, ordering, and result-defining budgets; define a typed aggregate service budget for rows/nodes/edges/occurrences/visited state/memory/deadline/cancellation/rendered output and derive typed per-batch database envelopes for returned rows, decoded/intermediate bytes, endpoint/purpose hydration, deadline, cancellation, and stable pagination. Keep one SQLite read snapshot across selector resolution, every frontier expansion, endpoint/purpose hydration, and final row reconstruction, and release it on success, truncation, cancellation, or failure. Keep task-appropriate TOON, typed graph/path, verbatim slice, and supported JSON output valid, deterministic, UTF-8 safe, and uniformly bounded under serial or parallel execution rather than limiting only returned rows or final rendered bytes. Paged graph/coverage output must expose returned, truncated, continuation, and typed exact/at-least/unknown total state; return an exact total only when already known, proved by the bounded page, or computable inside the same query budget, never by an otherwise unnecessary full high-degree count.
- [ ] 5.8 Close the agent-first navigation and analysis section with the required architecture-and-pre-mortem reconciliation; verify purpose-plus-graph discovery, anchored file/symbol relation traversal, SQLite one-hop and Rust multi-hop ownership, curator handoff, ranking, budgets/cursors, result shapes, next calls, and future v0.4 skill routing against live MCP behavior and representative agent workflows before enrichments or federation proceed.

## 6. Enrichments, Explicit Federation, And Optional Capabilities

- [ ] 6.1 Add one versioned accepted relation-family inventory and independently gated typed structural/type, package/manifest, test, route/protocol, configuration/environment, deployment/infrastructure, bounded static read/write, and optional inferred similarity/co-change relationships with source context, ambiguity, secret exclusion, invalidation, derived settings/query/docs/fixture coverage, and adversarial negative fixtures; include bounded caller-argument/request-field to handler-parameter/request-field context only where statically supported, and reject silent loss or weakening of accepted rows.
- [ ] 6.2 Add explicit ordered-root, call-only, read-only federation for approved relation/analysis calls with project-qualified identities, all-roots fail-closed validation, hard participating-root and simultaneously open snapshot/connection limits, aggregate database/input/row/edge/intermediate-byte/time/output/cancellation-to-close budgets, bounded in-memory resolution, and no persisted roots, cache, telemetry/settings, active-project change, leaked handle, or cross-project write; cover many roots, one invalid late root, generation change, and cancellation without partial rows.
- [ ] 6.3 Add an optional semantic retrieval lifecycle whose install, enable, build, ready, stale, update, rollback, disable, failure, and removal states cannot affect structural/lexical publication or default-core operation; select ANN/model composition only after labeled retrieval quality, determinism, update-cost, memory, package, and platform checks.
- [ ] 6.4 Add safe derived-graph snapshot export/import: use SQLite backup only for a private consistent capture, construct the distributable artifact freshly from an explicit allowlist of derived tables/columns plus a content inventory, and prove no project identity, purposes, health resolutions, telemetry, settings, future Memory Atlas state, secret-bearing raw values, machine-local paths, or deleted/free-page remnants escape. Apply bounded archive/root/schema/digest/source-state/capability checks and an optional explicit signature trust policy for shared snapshots without burdening local-only use; import through isolated validation and normal atomic derived-generation publication while preserving authored data and destination identity.
- [ ] 6.5 Close the enrichment, federation, and optional-capability section with the required architecture-and-pre-mortem reconciliation; verify relation-family ownership, cross-root read isolation and aggregate limits, optional semantic/parser lifecycle containment, snapshot trust and authored-state exclusion, cancellation/failure cleanup, and default-core independence against live source and adversarial fixtures before final release verification.

## 7. Integrated Behavior And Issue Completion

- [ ] 7.1 Add focused owning unit/integration tests plus risk-required real CLI/MCP smoke, concurrency, corruption, Unicode, cancellation, affected-platform E2E, query-plan, benchmark, and profile checks for the coherent behavior slices above; database behavior must include real temporary SQLite writes/reads/reopen through production schema, migration, purpose projection, publication/read snapshot, affected-closure, endpoint hydration, WAL/busy/checkpoint, backup/recovery, and adapter paths rather than mocks or SQL-text-only assertions; reuse one test across related tasks when it proves the behavior and do not duplicate every test layer without a distinct boundary risk.
- [ ] 7.2 Freeze and preserve the complete pre-v0.4 MCP inventory, names, schemas, defaults, and behavior while graph construction, freshness, purpose-plus-connection enrichment, direct relation navigation, and next-call guidance remain automatic behind existing calls; add no mandatory graph, jump, or orchestration tool. Record the packaged v0.4 inventory and discovery measurements as the later issue #310 baseline, but defer public tool classification, compact/default inventory selection, alias consolidation, breaking removal, router/installer/plugin inventory changes, and inventory-specific agent evaluation to #310.
- [ ] 7.3 Use the candidate as the main agent's first navigation tool on representative dirty-worktree, clean, and non-git tasks; forward-test the packaged v0.4 ProjectAtlas skill as the routing authority and require agents to select the correct existing MCP call and next call from task context without leaked implementation guidance. Require correct fresh source selection through purpose-plus-crisp-connections before summary, then trust and exact slice, with no regression in calls, reads, backtracking, emitted bytes, or total context against 0.3.26 and the stronger predefined comparison targets. After task 5.5 stabilizes the actual MCP relation/navigation surface, revalidate every complete-support candidate's machine-checkable profile and bounded agent-navigation evidence against that final surface, obtain independent review, promote only passing rows into the accepted complete-support inventory, and leave every other row at its strongest independently proven lower tier.
- [ ] 7.4 Pass focused checks, ordinary locked Rust/workspace gates, source/dependency policy, strict OpenSpec, and IssueOps synchronization on the combined #308 surface; resolve bounded Rust/storage/performance/security/platform/KISS/agent-workflow reviews; run the preregistered small/medium/huge local-source matrix, including a pinned real-source huge corpus, and reject accidental hot-query plan scans, relation/endpoint/purpose statement amplification, repeated whole-tree/node-table work on unchanged healthy-epoch reads, unbounded materialization, filesystem-read or incremental/full-rebuild amplification, correctness-fallback search work that exceeds declared selected-text byte/time bounds, excessive publication transaction hold time, worker oversubscription, queue/backpressure or lock/busy contention, cancellation that does not quiesce promptly, retained worker/RSS growth after repeated work, or unexplained regressions in scan/refresh/query/startup time, CPU/parallel efficiency, memory/RSS, database/WAL/staging writes and checkpoint behavior, persistent bytes, or output size.
  - Final issue-only quality gate: after every #308 functional task and agent workflow is complete, use `cargo llvm-cov` to drive the changed functional Rust surface toward near-100% line coverage and run `cargo mutants` against that final surface; resolve behavior-relevant uncovered lines and surviving mutants or document precise unreachable/platform-only/generated exclusions. Do not run this gate between slice commits and do not produce per-task receipts.
- [ ] 7.5 Perform the final combined architecture-and-pre-mortem reconciliation and reconcile `docs/agent-navigation.md`, the existing architecture documentation, the accepted complete-support inventory plus generated Markdown/Pages catalog identity and landing links, generated capability/user/agent documentation, and every live issue #308 failure mode/mitigation against validated behavior; keep concise GitHub-rendered Mermaid diagrams for crate/component ownership, language registry/parser/fact/provider/SQLite/MCP navigation, MCP communication, verified source-observation epoch and invalidation, database authority/physical model/bounded read/lifecycle, staged transactional publication and WAL behavior, initial purpose-plus-graph discovery, anchored file/symbol connection traversal, the host-owned background purpose-curator lane, the future independently revised/capped authored Memory Atlas boundary, and failure/cancellation/retry flows; render and visually plus semantically review every changed view, retire stale risks rather than preserving ceremony, enforce one lean issue `Architecture Diagrams` section with durable links to the versioned Markdown views, delete the fully recovered old #308 branches/worktrees, synchronize the completed checklist and derived mitigation states, and close the issue before #314 and #311 proceed.
