## 0. Execution Order

Work in dependency order. Establish task 2.0's database architecture baseline before further functional implementation; at every later task transition, identify and complete the smallest required schema/key/index/query/transaction delta plus real SQLite write/read coverage before dependent Rust service or CLI/MCP behavior. Land significant compiling behavior slices into `dev` after focused behavior checks and ordinary locked Rust/workspace gates pass. Every non-trivial Rust slice and implementation/review subagent must assess intended-scale algorithmic, allocation/data-layout, filesystem-I/O, SQLite schema/index/query-plan/transaction/WAL, incremental-work, CPU/worker/concurrency, lock/backpressure/cancellation, memory/RSS, startup, persistent-size, and output performance; add a focused query-plan assertion, benchmark, or profile when inspection and behavior tests cannot establish the risk. A single coherent test may cover several related tasks. Do not add per-task tests, receipts, SHA ledgers, rendered evidence, issue sealing, or between-slice mutation/coverage machinery. Run one combined final changed-surface line-coverage and mutation audit only after the complete #308 behavior stabilizes at task 7.4.

## 1. Lean Planning And Source Recovery

- [x] 1.1 Replace the old evidence-driven #308 plan with lean proposal, design, capability specs, and behavior-slice tasks that preserve the accepted product scope.
- [x] 1.2 Map the change to issue #308, replace the stale GitHub status/checklist with this exact task list, and pass strict OpenSpec plus IssueOps validation.
- [x] 1.3 Review every retained old #308 branch and dirty worktree plus every functional requirement in the last complete 200-plus-task #308 specification; classify each old requirement as retained, improved, implemented, mapped to an open owning task, intentionally rejected with a durable product reason, or missing; add any missing product behavior to the smallest owning current spec/task, reject obsolete evidence/workflow machinery, and retire redundant branches/worktrees after the recovery decisions are recorded.
- [x] 1.4 Freeze representative 0.3.26 agent workflows and tasks for startup, purpose-led folder/file selection, inspect/summary, relations, and exact slice so navigation improvement can be evaluated without changing the normal funnel.

## 2. Typed Graph And Safe Storage

- [x] 2.0 Validate, document, and reconcile the ProjectAtlas database architecture against the future local-first workload before further graph mutation work: decide SQLite versus realistic embedded/server/graph alternatives; define engine/version/driver and local-filesystem/durability operating assumptions, authored/derived authority, conceptual/logical/physical entities, stable keys/constraints, purpose projection, hot query and index ownership, prepared/batched access, publication/read transaction boundaries, WAL/busy/checkpoint/statistics/concurrency behavior, migrations/backup/recovery, huge-source scale and resource limits, measurable engine-invalidation conditions, and the verification matrix; inspect the live schema, queries, publication, and recovery implementation and either correct every mismatch or assign it to the smallest owning unchecked task; add focused Mermaid authority, physical-model, publication, and bounded-read views, render and visually review them, and obtain independent Rust/database/SQLite architecture review without changing the accepted seven-crate boundary unless a real owner and consumer are proven.
- [x] 2.1 Add responsibility-owned typed graph identities, entities, logical relations, retained source occurrences, resolution, confidence, coverage, generation, reusable target selectors, and limits while preserving legacy relation compatibility and failing closed on stable-key collisions.
  - Shared behavior coverage: [`project_identity_validates_and_round_trips`](https://github.com/styler-ai/ProjectAtlas/blob/dev/crates/projectatlas-core/src/graph.rs#L1735), [`entity_keys_are_stable_line_independent_and_project_scoped`](https://github.com/styler-ai/ProjectAtlas/blob/dev/crates/projectatlas-core/src/graph.rs#L1766), [`stable_key_collisions_fail_closed`](https://github.com/styler-ai/ProjectAtlas/blob/dev/crates/projectatlas-core/src/graph.rs#L1796), [`selectors_normalize_slashes_and_preserve_unicode_and_case`](https://github.com/styler-ai/ProjectAtlas/blob/dev/crates/projectatlas-core/src/graph.rs#L1826), [`invalid_selector_inputs_are_rejected`](https://github.com/styler-ai/ProjectAtlas/blob/dev/crates/projectatlas-core/src/graph.rs#L1861), [`logical_relations_deduplicate_distinct_source_occurrences`](https://github.com/styler-ai/ProjectAtlas/blob/dev/crates/projectatlas-core/src/graph.rs#L1926), [`relation_records_reject_inconsistent_identity_resolution_and_generation`](https://github.com/styler-ai/ProjectAtlas/blob/dev/crates/projectatlas-core/src/graph.rs#L1975), [`ambiguous_candidate_counts_do_not_change_logical_relation_identity`](https://github.com/styler-ai/ProjectAtlas/blob/dev/crates/projectatlas-core/src/graph.rs#L2171), [`relation_resolution_and_target_selectors_round_trip`](https://github.com/styler-ai/ProjectAtlas/blob/dev/crates/projectatlas-core/src/graph.rs#L2207), [`legacy_relation_projection_remains_compatible`](https://github.com/styler-ai/ProjectAtlas/blob/dev/crates/projectatlas-core/src/graph.rs#L2311), [`coverage_state_enforces_consistent_counts_and_reasons`](https://github.com/styler-ai/ProjectAtlas/blob/dev/crates/projectatlas-core/src/graph.rs#L2330), and [`graph_limits_are_nonzero_bounded_and_validated_on_input`](https://github.com/styler-ai/ProjectAtlas/blob/dev/crates/projectatlas-core/src/graph.rs#L2409). These tests jointly cover the typed graph contract; they are not one-test-per-task receipts.
- [x] 2.2 Add read-only database/root/schema preflight and append-only migration ownership that preserves project identity, purposes, review state, settings, and telemetry on supported upgrades and refuses incompatible state without mutation.
  - Shared behavior coverage: [`validated_existing_database_paths_are_never_recreated`](https://github.com/styler-ai/ProjectAtlas/blob/dev/crates/projectatlas-db/src/lib.rs#L4983), [`schema_upgrade_preserves_local_state_and_restarts_publication`](https://github.com/styler-ai/ProjectAtlas/blob/dev/crates/projectatlas-db/src/lib.rs#L5030), [`schema_lookalikes_are_refused_without_mutation`](https://github.com/styler-ai/ProjectAtlas/blob/dev/crates/projectatlas-db/src/schema.rs#L1266), [`writable_connections_enforce_declared_foreign_keys`](https://github.com/styler-ai/ProjectAtlas/blob/dev/crates/projectatlas-db/src/schema.rs#L1395), [`active_wal_schema_eight_is_refused_without_migration`](https://github.com/styler-ai/ProjectAtlas/blob/dev/crates/projectatlas-db/src/schema.rs#L1935), [`concurrent_migrators_converge_without_global_coordination`](https://github.com/styler-ai/ProjectAtlas/blob/dev/crates/projectatlas-db/src/schema.rs#L2100), [`root_and_metadata_validation_flow`](https://github.com/styler-ai/ProjectAtlas/blob/dev/crates/projectatlas-cli/tests/e2e.rs#L4468), and [`no_telemetry_readonly_cli_smoke`](https://github.com/styler-ai/ProjectAtlas/blob/dev/crates/projectatlas-cli/tests/e2e.rs#L4947). These tests jointly cover the storage boundary; they are not one-test-per-task receipts.
- [x] 2.3 Implement validated bounded full/incremental staging that performs filesystem reads, hashing, parsing, graph extraction, and summary derivation before acquiring the main SQLite writer; revalidate source/configuration/base generation, then apply only prepared batched database mutation inside one short `BEGIN IMMEDIATE` publication transaction that keeps the last valid generation queryable and rolls back completely. Keep small admitted work in bounded typed Rust batches and select a bounded disposable working file, implemented as a plain file or SQLite-backed spool, only when measured cardinality, memory, recovery, and write-amplification behavior justify spill; even when SQLite-backed, the spool is not a ProjectAtlas database, has no project identity, authored state, active generation, or query surface, and is removed after publish/failure/restart. Do not restore physical slotting, copied live databases, or retained rollback generations unless a measured recovery/concurrency need justifies them.
  - Shared behavior coverage: [`symbol_build_clamps_file_bytes_and_bounds_all_published_output`](https://github.com/styler-ai/ProjectAtlas/blob/dev/crates/projectatlas-cli/src/runtime.rs#L6291), [`publication_revalidation_rejects_source_policy_and_import_drift`](https://github.com/styler-ai/ProjectAtlas/blob/dev/crates/projectatlas-cli/src/runtime.rs#L6800), [`canceled_watcher_batch_preserves_last_valid_and_retries_one_generation`](https://github.com/styler-ai/ProjectAtlas/blob/dev/crates/projectatlas-cli/src/runtime.rs#L7108), [`read_snapshots_expose_only_complete_publications`](https://github.com/styler-ai/ProjectAtlas/blob/dev/crates/projectatlas-db/src/lib.rs#L5474), [`stale_publication_base_is_rejected_before_batch_mutation`](https://github.com/styler-ai/ProjectAtlas/blob/dev/crates/projectatlas-db/src/lib.rs#L5551), and [`index_adapters_publish_scan_symbol_and_watch_effects`](https://github.com/styler-ai/ProjectAtlas/blob/dev/crates/projectatlas-cli/src/mcp.rs#L5205). These shared tests jointly cover aggregate retained-byte admission, off-writer staging under same-database contention, exact source/configuration/purpose revalidation, runtime and storage generation compare-and-swap with winner preservation, unfinished replacement refusal, late rollback, old/new WAL reader visibility, watcher retry and descendant invalidation, and real synchronous/background MCP adapters; they are not one-test-per-task receipts.
- [x] 2.4 Implement one-transaction incremental structural deltas, with affected-row invalidation, one generation advance, and complete rollback on late failure.
  - Shared behavior coverage: [`unchanged_events_do_not_advance_publication_generation`](https://github.com/styler-ai/ProjectAtlas/blob/dev/crates/projectatlas-cli/src/runtime.rs#L4519), [`changed_and_deleted_paths_publish_one_complete_generation`](https://github.com/styler-ai/ProjectAtlas/blob/dev/crates/projectatlas-cli/src/runtime.rs#L4556), [`extended_windows_watch_roots_keep_deleted_paths_in_scope`](https://github.com/styler-ai/ProjectAtlas/blob/dev/crates/projectatlas-cli/src/runtime.rs#L4619), [`notify_events_normalize_unicode_paths_against_extended_windows_roots`](https://github.com/styler-ai/ProjectAtlas/blob/dev/crates/projectatlas-cli/src/runtime.rs#L4632), [`notify_events_preserve_native_backslash_paths_on_unix`](https://github.com/styler-ai/ProjectAtlas/blob/dev/crates/projectatlas-cli/src/runtime.rs#L4674), [`watcher_filters_relevant_index_events`](https://github.com/styler-ai/ProjectAtlas/blob/dev/crates/projectatlas-cli/src/main.rs#L3260), [`read_snapshots_expose_only_complete_publications`](https://github.com/styler-ai/ProjectAtlas/blob/dev/crates/projectatlas-db/src/lib.rs#L5308), and [`normal_reads_do_not_serve_offline_stale_index_state`](https://github.com/styler-ai/ProjectAtlas/blob/dev/crates/projectatlas-cli/tests/e2e.rs#L7457). These tests jointly cover affected-path publication, rollback/snapshot visibility, no-change behavior, watcher compatibility, Unicode, and native Windows/Unix path normalization.
- [x] 2.5 Persist and query typed graph, source-context, coverage, and lexical rows through indexed prepared operations that propagate corruption/iteration failures and avoid whole-graph scans or generic JSON hot paths.
  - Shared behavior coverage: [`affected_graph_replacement_preserves_only_the_unaffected_closure`](https://github.com/styler-ai/ProjectAtlas/blob/dev/crates/projectatlas-db/src/repository_graph.rs#L2468), [`typed_graph_round_trips_through_bounded_indexed_queries`](https://github.com/styler-ai/ProjectAtlas/blob/dev/crates/projectatlas-db/src/repository_graph.rs#L3066), [`graph_queries_fail_closed_on_corrupt_normalized_rows`](https://github.com/styler-ai/ProjectAtlas/blob/dev/crates/projectatlas-db/src/repository_graph.rs#L3320), and [`graph_publication_failure_rolls_back_text_graph_and_generation_for_readers`](https://github.com/styler-ai/ProjectAtlas/blob/dev/crates/projectatlas-db/src/repository_graph.rs#L3544). These shared tests cover indexed bounded reads, normalized corruption rejection, case-sensitive affected-closure replacement, final-occurrence relation invalidation, manifest/occurrence/coverage invalidation, candidate-bounded external cleanup, root replacement, graph-generation ownership, lexical/legacy compatibility, rollback, and reader snapshots.
- [x] 2.6 Implement explicit verified root-move and copy/detach behavior so independent worktrees or clones cannot collapse project identity and snapshots cannot overwrite destination identity.
  - Shared behavior coverage: [`root_transitions_preserve_authored_state_and_isolate_copies`](https://github.com/styler-ai/ProjectAtlas/blob/dev/crates/projectatlas-db/src/project_identity.rs#L441), [`root_and_metadata_validation_flow`](https://github.com/styler-ai/ProjectAtlas/blob/dev/crates/projectatlas-cli/tests/e2e.rs#L4468), and [`mcp_project_path_overrides_keep_projects_isolated`](https://github.com/styler-ai/ProjectAtlas/blob/dev/crates/projectatlas-cli/src/main.rs#L4028). These shared tests jointly cover destination validation, bind/move/detach identity semantics, supported migration, exact authored-state preservation, graph/publication isolation, rejected and rollback immutability, platform-aware absence proof, real CLI move and follow-up repair, MCP compatibility, and unchanged tool inventory.
- [x] 2.7 Map every existing and planned database-backed MCP/CLI call to its exact captured source-root/database/configuration binding plus bounded physical read/write, purpose-projection, freshness, generation, selector, trust/coverage, transaction, and output-byte contract; never auto-discover, attach, merge, substitute, fall back to, or combine another database inside one request; enforce the selected local-filesystem SQLite profile before writable WAL access, rejecting known unsupported or uncertain live network-filesystem state without mutation or a silent durability downgrade; audit and adapt the existing 0.4 storage tests so database behavior writes through real production transactions and reads or reopens through real production query/service/adapter paths. Use in-memory SQLite only where filesystem, WAL, migration, reopen, backup, busy/concurrency, and platform behavior cannot matter; use temporary on-disk production `AtlasStore` plus real CLI/MCP smoke otherwise. Mocked repositories, hand-built result rows, or SQL-text-only assertions do not prove persistence, query-plan, rollback, or agent-visible behavior.
  - Shared behavior coverage: [`read_snapshots_expose_only_complete_publications`](https://github.com/styler-ai/ProjectAtlas/blob/dev/crates/projectatlas-db/src/lib.rs#L5809), [`writable_mutators_reject_an_active_read_snapshot`](https://github.com/styler-ai/ProjectAtlas/blob/dev/crates/projectatlas-db/src/lib.rs#L5913), [`read_only_telemetry_revalidates_project_identity_before_write`](https://github.com/styler-ai/ProjectAtlas/blob/dev/crates/projectatlas-db/src/lib.rs#L6594), [`ordinary_current_open_skips_full_scan_while_explicit_verify_checks_integrity`](https://github.com/styler-ai/ProjectAtlas/blob/dev/crates/projectatlas-db/src/schema.rs#L1570), [`current_schema_requires_complete_project_binding_without_repair`](https://github.com/styler-ai/ProjectAtlas/blob/dev/crates/projectatlas-db/src/schema.rs#L1602), [`current_writer_revalidates_binding_captured_by_preflight`](https://github.com/styler-ai/ProjectAtlas/blob/dev/crates/projectatlas-db/src/schema.rs#L2087), [`stale_stores_cannot_write_after_binding_transitions`](https://github.com/styler-ai/ProjectAtlas/blob/dev/crates/projectatlas-db/src/project_identity.rs#L824), [`stored_health_resolution_filter_stays_indexed_and_bind_bounded`](https://github.com/styler-ai/ProjectAtlas/blob/dev/crates/projectatlas-db/src/lib.rs#L7009), [`rejected_database_location_does_not_create_or_change_database`](https://github.com/styler-ai/ProjectAtlas/blob/dev/crates/projectatlas-cli/src/runtime.rs#L5994), [`cli_database_filesystem_failures_are_typed_in_json_and_toon`](https://github.com/styler-ai/ProjectAtlas/blob/dev/crates/projectatlas-cli/src/main.rs#L3498), [`mcp_database_filesystem_failures_are_typed_and_actionable`](https://github.com/styler-ai/ProjectAtlas/blob/dev/crates/projectatlas-cli/src/mcp.rs#L4549), [`index_adapters_publish_scan_symbol_and_watch_effects`](https://github.com/styler-ai/ProjectAtlas/blob/dev/crates/projectatlas-cli/src/mcp.rs#L5282), [`mcp_server_stays_bound_to_one_project_database`](https://github.com/styler-ai/ProjectAtlas/blob/dev/crates/projectatlas-cli/tests/e2e.rs#L4846), and [`root_and_metadata_validation_flow`](https://github.com/styler-ai/ProjectAtlas/blob/dev/crates/projectatlas-cli/tests/e2e.rs#L4473). These tests jointly cover the production local-filesystem/WAL profile, complete-generation readers, bounded current-schema opens, post-open root and identity revalidation, same-root identity rotation, stale-writer rejection across all mutation boundaries, read-snapshot mutation refusal, explicit full verification, no-mutation rejection, typed CLI/MCP failures, indexed health filtering, real adapter readback, exact root/database/configuration isolation, and compatibility behavior; they are not one-test-per-task receipts.
- [ ] 2.8 Bound the single project database and temporary-work lifecycle before graph volume grows further: replace the eternal implicit telemetry session with a bounded internal runtime/invocation instance distinct from an optional caller label; preserve exact session-scope `max(baseline without ProjectAtlas) - sum(emitted with ProjectAtlas)` deduplication while the instance is active, never silently reopen deleted baseline state after sealing/expiry, and keep global totals exact while session detail reports retained/partial/expired/unavailable. Retain recent raw usage telemetry under explicit row/age/logical-byte budgets, compact older measurements into bounded typed aggregates without changing supported token-report totals and declared trends, expose expired raw-detail and content-free retention/maintenance state honestly, remove only ownership-validated abandoned spill artifacts and obsolete derived state, and define measured WAL/checkpoint/statistics/free-page reuse or reclaim behavior without unbounded cleanup or blocking `VACUUM` on normal agent reads. Prove retention, aggregation, authored-state preservation, page reuse/reclaim, and real settings/token-report behavior with temporary on-disk production SQLite plus the real service/CLI/MCP paths; when a measured production spill owner exists, also prove ownership-validated crash/restart cleanup, otherwise prove an explicit no-spill/not-applicable state whose restart maintenance leaves arbitrary lookalike files untouched.

## 3. Freshness, Invalidation, And Bounded Work

- [x] 3.1 Make every normal index-backed read, watch, and one-shot refresh detect relevant local edits, deletes, renames, ignore/configuration changes, and optional VCS-state changes after edits or restart; reconcile a safe bounded delta or return typed `refresh_required` instead of serving known-stale file, summary, symbol, relation, or search results in git or non-git source trees.
  - Shared behavior coverage: [`normal_reads_do_not_serve_offline_stale_index_state`](https://github.com/styler-ai/ProjectAtlas/blob/dev/crates/projectatlas-cli/tests/e2e.rs#L7457), [`normal_read_refresh_delta_enforces_path_and_byte_budgets`](https://github.com/styler-ai/ProjectAtlas/blob/dev/crates/projectatlas-cli/src/runtime.rs#L4412), [`automatic_read_refresh_returns_typed_state_when_writer_is_unavailable`](https://github.com/styler-ai/ProjectAtlas/blob/dev/crates/projectatlas-cli/src/runtime.rs#L4460), [`lint_purpose_levels_require_agent_review_at_configured_scope`](https://github.com/styler-ai/ProjectAtlas/blob/dev/crates/projectatlas-cli/tests/e2e.rs#L9636), [`projection_refresh_cannot_replace_publication_contract`](https://github.com/styler-ai/ProjectAtlas/blob/dev/crates/projectatlas-db/src/lib.rs#L5275), [`mcp_project_path_overrides_keep_projects_isolated`](https://github.com/styler-ai/ProjectAtlas/blob/dev/crates/projectatlas-cli/src/main.rs#L4036), and [`skipped_symbol_builds_invalidate_stale_symbols`](https://github.com/styler-ai/ProjectAtlas/blob/dev/crates/projectatlas-cli/tests/e2e.rs#L9962). These tests jointly cover Git and non-Git offline edits, same-size/same-mtime bytes, add/rename/delete and policy drift, CLI/MCP routing, stale-purpose lint, bounded automatic reconciliation, writer contention, last-valid visibility, exact generation behavior, symbol invalidation, and fail-closed publication compatibility; they are not one-test-per-task receipts.
- [ ] 3.2 Preserve last-valid facts on transient path/root/database/permission uncertainty and re-resolve inbound dependents when exported identities or dependency keys change; persist typed indexed canonical export/resolution keys and relation dependency keys so old/new keys find both resolved inbound edges and previously unresolved references without display-name scans, and use set-oriented or bounded batched affected-source lookup rather than one query per endpoint before atomic publication or typed full-refresh escalation.
- [ ] 3.3 Prove create, modify, move, rename, delete, ignore/unignore, parser/configuration change, interruption, and unchanged-dirty sequences converge to clean-scan graph, coverage, and lexical results without repeated no-change publication.
- [x] 3.4 Keep exact-byte indexing and freshness verification cancellable and resource-bounded, preserve last-valid queries during failed work, leave failed or canceled watcher batches eligible for retry, and allow isolated projects to progress without one process-global lock; when isolated workers/packs exist, derive an effective resource envelope from configured safe limits plus supported host/container/job constraints.
  - Shared behavior coverage: [`work_control_shares_cancellation_and_deadline`](https://github.com/styler-ai/ProjectAtlas/blob/dev/crates/projectatlas-core/src/index_work.rs#L311), [`controlled_scan_enforces_entry_and_byte_limits_without_partial_results`](https://github.com/styler-ai/ProjectAtlas/blob/dev/crates/projectatlas-fs/src/lib.rs#L825), [`exact_hash_loop_observes_cancellation_between_chunks`](https://github.com/styler-ai/ProjectAtlas/blob/dev/crates/projectatlas-fs/src/lib.rs#L906), [`controlled_extraction_consumes_cancellation_and_preserves_compatibility`](https://github.com/styler-ai/ProjectAtlas/blob/dev/crates/projectatlas-symbols/src/lib.rs#L1953), [`background_task_envelope_and_cancellation_reach_owned_control`](https://github.com/styler-ai/ProjectAtlas/blob/dev/crates/projectatlas-cli/src/mcp.rs#L4952), [`background_scan_defers_config_validation_and_rejects_root_redirection`](https://github.com/styler-ai/ProjectAtlas/blob/dev/crates/projectatlas-cli/src/mcp.rs#L5137), [`index_adapters_publish_scan_symbol_and_watch_effects`](https://github.com/styler-ai/ProjectAtlas/blob/dev/crates/projectatlas-cli/src/mcp.rs#L5205), [`operation_deadline_starts_with_default_or_shorter_explicit_timeout`](https://github.com/styler-ai/ProjectAtlas/blob/dev/crates/projectatlas-cli/src/runtime.rs#L5368), [`purpose_import_inputs_observe_cancellation_limits_and_rollback`](https://github.com/styler-ai/ProjectAtlas/blob/dev/crates/projectatlas-cli/src/runtime.rs#L5784), [`read_snapshots_expose_only_complete_publications`](https://github.com/styler-ai/ProjectAtlas/blob/dev/crates/projectatlas-db/src/lib.rs#L5323), and [`canceled_watcher_batch_preserves_last_valid_and_retries_one_generation`](https://github.com/styler-ai/ProjectAtlas/blob/dev/crates/projectatlas-cli/src/runtime.rs#L6357). These shared tests jointly cover fixed host/process background-worker partitioning, bounded admission, shared scan/parser/control ceilings, aggregate purpose-input budgeting, exact-byte cancellation, specialized and fallback parser compatibility/cancellation, production synchronous and background scan/symbol/watch adapters, whole-operation deadlines, last-valid snapshot visibility, same-database contention, independent-database progress, and real watcher rollback/polling retry; they are not one-test-per-task receipts.
- [ ] 3.5 Prevent freshness from defeating bounded agent queries: in a long-lived MCP/watch runtime, activate relevant root/policy observation before or during the first exact post-start verification, reconcile buffered events, and establish a process-scoped verified source-observation epoch; bind database snapshots, results, and cursors to that epoch plus active generation, reuse it for later unchanged calls without another whole-tree walk or full node-table load, and invalidate it on relevant events, observer overflow/gap/disconnect, root/policy uncertainty, cancellation, or a source change during the query. Reconcile, retry, exactly re-verify, or return typed `refresh_required` before claiming current facts; keep one-shot CLI first verification honest. Prove restart after offline dirty/non-Git changes, watcher continuity failure, mid-query edits, cancellation, and repeated unchanged folder/file/summary/relation calls, measuring filesystem entries/bytes, SQLite statements/decoded nodes, latency, and output so the second and later healthy-epoch calls remain proportional to their bounded query rather than repository size.

## 4. Language Capability And Resolution

- [ ] 4.1 Generate deterministic detection, parser ownership, fixtures, tiers, settings, and documentation inputs from one versioned language capability registry and accepted capability-set manifest with honest detected/parsed/symbols/semantic/benchmarked states, derived counts, and no silent loss or weakening of accepted rows.
- [ ] 4.2 Preserve every current built-in language and parser behavior while adding deterministic exact-filename, compound-extension, extension, content/dialect, and explicit-override precedence.
- [ ] 4.3 Keep broad grammar/parser capabilities in an explicit supply-chain-verified optional pack with pinned provenance/digest/license/ABI, offline normal use, supervised out-of-process or capability-denied WASM/native containment, hard resource/cancellation and progress-aware no-progress limits, no repository execution, and no default-core download, compilation, linkage, or initialization; implement lifecycle only alongside a selected consuming runtime and preserve the MCP process plus active generation on pack failure.
- [ ] 4.4 Add normalized project-wide registries and independently structured semantic providers with resolved, ambiguous, unresolved, and external outcomes plus non-vacuous positive and negative fixtures for every advertised family; accept typed compiler metadata only as bounded non-executable, secret-redacted data where required; include bounded host-to-embedded byte/line/span reconciliation for accepted HTML-like, component, and template hosts.
- [ ] 4.5 Make existing settings report schema/migration compatibility, active generation, optional selected-slot detail, registry/provider/relation digests, actionable coverage, lexical/FTS/search state, optional-pack lifecycle, and the validated content-free SQLite operating profile—runtime version/compile-feature identity, supported/unsupported/uncertain filesystem profile, journal/synchronous/busy/checkpoint/statistics policy—from validated state without secrets or new machine-local paths; derive capability matrices, provenance/license inventory, and public support claims from the same authority.

## 5. Agent-First Navigation And Analysis

- [ ] 5.1 Extend the purpose queue with coalesced task/generation/path-scoped rows and update packaged host-neutral guidance/agent profiles to run isolated low-reasoning `low`-scope curation beside the main task at startup/relevant transitions when supported; keep successful maintenance out of normal responses, never start `medium`/`strict` implicitly, and never write SQLite directly.
- [ ] 5.2 Enrich existing folder and file rows before summary with reviewed one-line purposes plus crisp bounded package/import/call/reference/test/route/config connections, while preserving exact path/name and strong purpose priority with deterministic compact reason codes.
- [ ] 5.3 Add deeper bounded relationship/coverage digests, opt-in project-wide coverage discovery with path, parser/provider, relation, state, and reason filters plus complete/partial/failed/ignored/oversized/quarantined/stale outcomes, and typed next-call hints to selected-file summaries/health surfaces so agents gain useful context without default edge dumps, extra mandatory calls, or an implicit scan.
- [ ] 5.4 Preserve deterministic lexical search semantics; for indexable literal/token shapes use FTS/BM25 only as a complete candidate superset followed by exact verification, while arbitrary regex/fuzzy/short/punctuation-sensitive or tokenizer/Unicode-unsafe shapes use the correctness-authoritative path/byte/time-bounded persisted-text fallback with searched-file/searched-byte/truncation state and measured work proportional to the selected text scope. Add explicit optional `semantic`/`hybrid` modes with typed unavailable-state errors, lexical-complete hybrid ranking, and differential fallback-equivalence coverage.
- [ ] 5.5 Extend existing relation queries additively with typed direction, extended family, depth, confidence, resolution, retained source occurrences, reusable exact target selectors/next calls, pagination, and hard limits while old requests retain legacy rows and ordering; an agent already anchored at a file or symbol must traverse ranked inbound/outbound connections directly without repeating overview/folder/file discovery; every returned local-source node or path step must project its authoritative reviewed owning file/folder purpose plus review/stale state without duplicating purpose ownership into derived graph rows, while external or unresolved nodes report purpose as not applicable or unavailable. Hydrate each page/path's unique endpoint selectors and nearest applicable file/folder purpose projections through bounded batch queries rather than per-relation/entity/purpose round trips; measure a separately indexed recursive CTE against a node-simple Rust frontier for multi-hop paths and keep the mechanism that best preserves plans, topology, cancellation, trust, and visited/edge/time/memory/output budgets.
- [ ] 5.6 Add bounded architecture, relationship-derived component/community candidate, dependency-cycle/SCC candidate, reviewed-purpose alignment/drift, language-valid complexity/bottleneck candidate, VCS-aware impact/dead-code candidate, and node-simple static trace views through existing summary/health/relation services; `trace` means bounded static relationship/path inspection over indexed source facts, not runtime execution-trace ingestion. Keep inferred architecture candidate-labeled, deterministic, selector-bearing, coverage-aware, opt-in when expensive, and explicitly truncated under row/edge/time/output budgets; add at most one optional closed analysis tool only if real agent tasks prove the relation request becomes less clear, and do not add a generic query language or fourth complexity tool. Shared behavior coverage must include a deterministic cross-folder community, matching and conflicting purpose cases, one real cycle plus an acyclic negative fixture, reusable source selectors, visible ambiguous/incomplete coverage, and explicit budget truncation.
- [ ] 5.7 Bind cursors to project/root, active generation, capability, query, filters, ordering, and result-defining budgets; keep task-appropriate TOON, typed graph/path, verbatim slice, and supported JSON output valid, deterministic, UTF-8 safe, and uniformly bounded under serial or parallel execution, including decoded/intermediate database bytes and endpoint/purpose hydration rather than only returned row counts or final rendered bytes. Paged graph/coverage output must expose returned, truncated, continuation, and typed exact/at-least/unknown total state; return an exact total only when already known, proved by the bounded page, or computable inside the same query budget, never by an otherwise unnecessary full high-degree count.

## 6. Enrichments, Explicit Federation, And Optional Capabilities

- [ ] 6.1 Add one versioned accepted relation-family inventory and independently gated typed structural/type, package/manifest, test, route/protocol, configuration/environment, deployment/infrastructure, bounded static read/write, and optional inferred similarity/co-change relationships with source context, ambiguity, secret exclusion, invalidation, derived settings/query/docs/fixture coverage, and adversarial negative fixtures; include bounded caller-argument/request-field to handler-parameter/request-field context only where statically supported, and reject silent loss or weakening of accepted rows.
- [ ] 6.2 Add explicit ordered-root, call-only, read-only federation for approved relation/analysis calls with project-qualified identities, all-roots fail-closed validation, hard participating-root and simultaneously open snapshot/connection limits, aggregate database/input/row/edge/intermediate-byte/time/output/cancellation-to-close budgets, bounded in-memory resolution, and no persisted roots, cache, telemetry/settings, active-project change, leaked handle, or cross-project write; cover many roots, one invalid late root, generation change, and cancellation without partial rows.
- [ ] 6.3 Add an optional semantic retrieval lifecycle whose install, enable, build, ready, stale, update, rollback, disable, failure, and removal states cannot affect structural/lexical publication or default-core operation; select ANN/model composition only after labeled retrieval quality, determinism, update-cost, memory, package, and platform checks.
- [ ] 6.4 Add safe derived-graph snapshot export/import: use SQLite backup only for a private consistent capture, construct the distributable artifact freshly from an explicit allowlist of derived tables/columns plus a content inventory, and prove no project identity, purposes, health resolutions, telemetry, settings, future Memory Atlas state, secret-bearing raw values, machine-local paths, or deleted/free-page remnants escape. Apply bounded archive/root/schema/digest/source-state/capability checks and an optional explicit signature trust policy for shared snapshots without burdening local-only use; import through isolated validation and normal atomic derived-generation publication while preserving authored data and destination identity.

## 7. Integrated Behavior And Issue Completion

- [ ] 7.1 Add focused owning unit/integration tests plus risk-required real CLI/MCP smoke, concurrency, corruption, Unicode, cancellation, affected-platform E2E, query-plan, benchmark, and profile checks for the coherent behavior slices above; database behavior must include real temporary SQLite writes/reads/reopen through production schema, migration, purpose projection, publication/read snapshot, affected-closure, endpoint hydration, WAL/busy/checkpoint, backup/recovery, and adapter paths rather than mocks or SQL-text-only assertions; reuse one test across related tasks when it proves the behavior and do not duplicate every test layer without a distinct boundary risk.
- [ ] 7.2 Add a closed `agent | full` MCP surface: installer-generated configs advertise the compact documented agent inventory within its discovery-byte budget, bare/manual full mode preserves every old name/schema/default, redundant aliases share services, and representative purpose-led local-source workflows prove no mandatory call growth or semantic drift.
- [ ] 7.3 Use the candidate as the main agent's first navigation tool on representative dirty-worktree, clean, and non-git tasks; require correct fresh source selection through purpose-plus-crisp-connections before summary, then trust and exact slice, with no regression in calls, reads, backtracking, emitted bytes, or total context against 0.3.26 and the stronger predefined comparison targets.
- [ ] 7.4 Pass focused checks, ordinary locked Rust/workspace gates, source/dependency policy, strict OpenSpec, and IssueOps synchronization on the combined #308 surface; resolve bounded Rust/storage/performance/security/platform/KISS/agent-workflow reviews; run the preregistered small/medium/huge local-source matrix, including a pinned real-source huge corpus, and reject accidental hot-query plan scans, relation/endpoint/purpose statement amplification, repeated whole-tree/node-table work on unchanged healthy-epoch reads, unbounded materialization, filesystem-read or incremental/full-rebuild amplification, correctness-fallback search work that exceeds declared selected-text byte/time bounds, excessive publication transaction hold time, worker oversubscription, queue/backpressure or lock/busy contention, cancellation that does not quiesce promptly, retained worker/RSS growth after repeated work, or unexplained regressions in scan/refresh/query/startup time, CPU/parallel efficiency, memory/RSS, database/WAL/staging writes and checkpoint behavior, persistent bytes, or output size.
  - Final issue-only quality gate: after every #308 functional task and agent workflow is complete, use `cargo llvm-cov` to drive the changed functional Rust surface toward near-100% line coverage and run `cargo mutants` against that final surface; resolve behavior-relevant uncovered lines and surviving mutants or document precise unreachable/platform-only/generated exclusions. Do not run this gate between slice commits and do not produce per-task receipts.
- [ ] 7.5 Reconcile `docs/agent-navigation.md`, the existing architecture documentation, and generated capability/user/agent documentation against validated behavior; keep concise GitHub-rendered Mermaid diagrams for crate/component ownership, MCP communication, verified source-observation epoch and invalidation, database authority/physical model/bounded read/lifecycle, staged transactional publication and WAL behavior, initial purpose-plus-graph discovery, anchored file/symbol connection traversal, the host-owned background purpose-curator lane, the future independently revised/capped authored Memory Atlas boundary, and failure/cancellation/retry flows; enforce one lean issue `Architecture Diagrams` section with durable links to the versioned Markdown views; delete the fully recovered old #308 branches/worktrees, synchronize the completed checklist, and close the issue before #314 and #311 proceed.
