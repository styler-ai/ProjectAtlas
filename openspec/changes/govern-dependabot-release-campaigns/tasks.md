## 1. Dependabot campaign automation (#498)

- [ ] 1.1 Implement and test the single `dependabot_campaign_issue` release-graph declaration plus idempotent bounded-body-region reconciliation for the complete release-window PR/audit union, exact record identity, retained history, final/non-final dispositions, optimistic mutation/readback, and failure on graph, milestone, relationship, inventory, or body drift.
- [ ] 1.2 Implement and test the smallest source-aware exact-Dependabot IssueOps path: event actor `dependabot[bot]`, authenticated PR-author readback `app/dependabot`, exactly one standalone non-closing campaign relationship, exact PR number/base/head/body/milestone/inventory binding, unchanged human ownership, review/thread/synchronization/protected-check readback, fresh explicit Sol merge-authorization dispatch, and no auto-merge, broad App/bot exemption, or weaker gate.
- [ ] 1.3 Implement and test one trusted default-branch `workflow_dispatch` audit launched through `gh workflow run`, driven solely and sequentially by `.github/dependabot.yml`, with least job-level permissions, official immutable Dependabot CLI/updater/proxy pins, sanitized proxy/log handling, `clean|findings|failed` ingestion into the same campaign region, and no REST/UI trigger, direct `dependabot-action`, parallel matrix, or second `workflow_run` framework; run the IssueOps self-test plus positive, negative, failure, compatibility, security, bounded-resource, locked Mermaid parser/CLI render, and visual-semantic gates, proving #494 narrows only for the complete contract.
- [ ] 1.4 Review the final implementation against the architecture diagrams, update the diagrams or implementation until they agree, or reconfirm the reasoned N/A.

## 2. v0.5.0 dependency campaign (#499)

- [ ] 2.1 After #498 is accepted on `main`, publish and read back #499 as the sole v0.5.0 campaign child, blocked by #498 and #482, and initialize the `2026-08-20T16:55:18Z`-through-close inventory with pending #453/#454/#455 and provisional accepted #457/#468 without creating per-PR issues.
- [ ] 2.2 After exact Rust #482 is accepted on `main`, refresh or rebase #453/#454/#455 onto current accepted `main`, rerun the shared affected-contract proof, and classify their current stale #314 branch-global IssueOps failures as baseline drift rather than dependency regressions before technical disposition.
- [ ] 2.3 Deliver and finally disposition #453 `object` with Rust/parser proof, then #454 `rusqlite` with the complete Rust/database/SQLite implementation-review stack and storage proof, then #455 `rmcp` with the complete Rust/MCP implementation-review stack and protocol proof; run and ingest the bounded audit before RC and again before stable; reconcile every release-window record to `accepted|deferred|declined|superseded`; preserve all required reviews, threads, protected contexts, exact campaign relationships, explicit Sol authorization, inventory/native-graph/hosted-proof readback, and no pending/provisional/failed state; and close #499 only when #492 release acceptance is unblocked.
- [ ] 2.4 Review the final implementation against the architecture diagrams, update the diagrams or implementation until they agree, or reconfirm the reasoned N/A.
