## 1. Dependabot campaign automation (#498)

- [ ] 1.1 Implement and test the single `dependabot_campaign_issue` release-graph declaration plus idempotent bounded-body-region reconciliation for the exact published campaign-contract activation revision and complete release-window PR/audit union, exact record identity, retained history, separate `retrospective_precontract` provenance and final/non-final dispositions, exact `collecting|candidate_ready|stable_ready` evidence, optimistic mutation/readback, and failure on graph, milestone, relationship, inventory, stage, or body drift.
- [ ] 1.2 Implement and test the smallest source-aware exact-Dependabot IssueOps path for every post-contract PR: event actor `dependabot[bot]`, authenticated PR-author readback `app/dependabot`, exactly one standalone non-closing campaign relationship, exact PR number/base/head/body/milestone/inventory binding, unchanged human ownership, review/thread/synchronization/protected-check readback, fresh explicit pre-merge Sol authorization, and no auto-merge, broad App/bot exemption, weaker gate, or retrospective escape; separately require pre-contract merged records to retain the historical gate gap and pass exact merge/main-inclusion readback, safe post-hoc relationship/milestone, fresh Sol merged-diff/current-main review, complete current-main protected/platform proof, and real corrective or superseding delivery when that proof fails.
- [ ] 1.3 Implement and test one trusted default-branch `workflow_dispatch` audit launched through `gh workflow run`, using a closed lossless translation of the current `.github/dependabot.yml` Cargo group, GitHub Actions entry, target branch, schedule, group/update types, and allow/ignore policies into sequential official pinned Dependabot CLI `update -f` jobs; fail unsupported/lossy fields; enforce positive per-entry/total timeout, output, and operation bounds plus cancellation/signal/container cleanup; keep checkout credentials unpersisted and the issues-write token absent from updater/proxy input, environment, mounts, logs, credentials, and Git config until every container exits and bounded sanitized output validates; ingest `clean|findings|failed` into the same region with no REST/UI trigger, direct `dependabot-action`, parallel matrix, or second `workflow_run`; run IssueOps plus positive, negative, failure, compatibility, credential-isolation, cleanup, bounded-resource, locked Mermaid parser/CLI render, and visual-semantic gates, proving #494 narrows only for the complete contract.
- [ ] 1.4 Review the final implementation against the architecture diagrams, update the diagrams or implementation until they agree, or reconfirm the reasoned N/A.

## 2. v0.5.0 dependency campaign (#499)

- [ ] 2.1 After #498 is accepted on `main`, publish and read back #499 as the sole v0.5.0 campaign child, blocked by #498 and #482, and initialize the `2026-08-20T16:55:18Z`-through-close inventory with pending #453/#454/#455 and provisional `retrospective_precontract` #457/#468 records containing exact dependency heads, merge commits, changed files, original five-check history, absent-at-merge review/milestone, and explicit historical gate gaps, without creating per-PR issues.
- [ ] 2.2 After exact Rust #482 is accepted on `main`, refresh or rebase #453/#454/#455 onto current accepted `main`, rerun the shared affected-contract proof, and classify their current stale #314 branch-global IssueOps failures as baseline drift rather than dependency regressions before technical disposition.
- [ ] 2.3 Deliver and finally disposition #453 `object` with Rust/parser proof, then #454 `rusqlite` with the complete Rust/database/SQLite implementation-review stack and storage proof, then #455 `rmcp` with the complete Rust/MCP implementation-review stack and protocol proof; reconcile #457/#468 through the full retrospective contract; reach exact `candidate_ready` from a successful pre-RC audit and final candidate inventory so RC1 may publish while #499/#492 stay open; after accepted RC1 continue intake, run the pre-stable audit, finally disposition every new record to `accepted|deferred|declined|superseded`, reach exact `stable_ready`, and close #499 so stable #492 acceptance unblocks, preserving every required review, thread, protected context, exact relationship, applicable Sol authorization, and inventory/native-graph/hosted-proof readback.
- [ ] 2.4 Review the final implementation against the architecture diagrams, update the diagrams or implementation until they agree, or reconfirm the reasoned N/A.
