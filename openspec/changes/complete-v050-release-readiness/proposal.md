## Why

ProjectAtlas v0.5.0 needs one coherent non-visual release campaign that repairs confirmed graph/path/platform failures, completes bounded repository intelligence, makes distribution truthful, and proves every installed CLI/MCP/host route before Memory Atlas begins. The milestone has independent implementation lanes but one feature-free release-acceptance owner: #492 is the native parent of every other v0.5 issue, is directly blocked by all of them, and closes last after RC and stable readback.

## What Changes

- Establish one native canonical project identity and lossless/versioned SQLite codec in #481; #484 reuses it for worktree root, Git common-directory, and Git administrative-directory identity.
- Keep graph identities strict while #476 classifies invalid parser-derived input into bounded typed coverage, and make #480 publish complete document graph results through bounded prepared chunks under one transaction/generation.
- Upgrade deterministically to exact Rust 1.98.0 from the historical 1.93.1 reproduction baseline. For v0.5, macOS Apple Silicon optional parsing is explicitly unavailable before mutation and built-in parsing remains supported; no unproven containment backend is added.
- Add built-in PHP 8 parsing with pinned `tree-sitter-php` 0.24.2, then derive the first version-matched PHP guidance profile from accepted evidence.
- Add PDF and DOCX extraction only, using pinned `pdf-extract` 0.12.0 and `docx-lite` 0.2.0 under strict admission/resource/provenance limits and sparse exact evidence.
- Make entrypoint reachability request-owned and non-persistent; replace weak-component communities with deterministic bounded weighted label-propagation v1; measure reverse-caller, graph-resource, and released-main-baseline proposals before accepting either a measured change or reproducible no-change result.
- Verify npm as a thin exact-native-runtime adapter, consume generated Claude Code/OpenCode configuration through real isolated host readers, and add one collision-safe `atlas` shim while preserving `projectatlas`, `health-check`, and administrative `health resolve`.
- Narrow release timeout work to the reported GitHub Actions step; split tests/modules only through accepted responsibility maps; remove the named oversized raw benchmark artifact and reuse the existing two-arm evaluation harness.
- Make pull-request CI lean without reducing proof: #497 uses one closed impact contract and one Python-standard-library planner for human and Dependabot pull requests, limits affected unions to ordinary additions/modifications, fails every rename/deletion plus unknown/shared/planner-owning inputs to full proof, and keeps default-branch, scheduled, candidate, and release proof complete. #498 adds one exact-author Dependabot campaign/audit automation contract with separate pre-PR audit-finding records that link to real PR records only after exact hosted readback, while #499 owns the sole v0.5.0 campaign issue and its two stage-bound release-window dispositions without per-PR issues or a second planner/store.
- Separate candidate-local specification validation from published readiness: after independent Sol and new hosted Codex review accept the corrected exact planning-PR head, primary Sol may publish/read back the thirty exact bodies while the PR remains open and the active graph remains the live twenty-five-child graph, solely so normal unfiltered IssueOps/CI can turn green. The temporary body-to-`main` architecture-link gap authorizes no readiness. Sol then merges/reads the planning artifacts, Luna lands/reviews the objective repository integration, Sol bootstraps/reads the hosted twenty-nine-child relationships, and a separate narrow atomic PR promotes only the exact graph/campaign while removing the manifest. `status:ready`, implementation handoff, and release remain fail-closed until exact published-main, live IssueOps, hosted graph, and semantic readback agree.
- Prevalidate every native relationship request, including an apparent no-op, against one exact graph-owned missing-or-extra transition before mutation; repair closed reverse dependents when blockers reopen; revoke affected merge authorization before other PR feedback using exact reread heads and repository-qualified closing references; treat demilestoning as targeted graph drift; and dynamically fail published-snapshot proof on wrong roots, malformed identities, Git failures, or default-branch movement while ignoring only untracked notes excluded from tracked state.
- Before freezing an RC candidate, make #492 read back the promoted twenty-nine-child graph plus every accepted issue's published OpenSpec task source and architecture URL/heading/Mermaid, require every implementation-bearing child except release-governance campaign #499 to be closed, and require #499 to be exact `candidate_ready` while #499/#492 stay open. Reconcile and safely execute every installed CLI command/nested command and MCP tool—including unchanged routes—run the holistic packaged product E2E, publish/read back RC1, return defects to owners, then require #499 `stable_ready` and closure before repeating proof for stable and closing #492 last.

Non-goals:

- No Ratatui graph explorer, token-dashboard extension, browser graph view, Memory Atlas, or v0.6 surface redesign.
- No new crate, generic parser/provider/guidance/timeout/batching/algorithm/action framework, second database, lossy identity, uncontained parser process, arbitrary office format, OCR, macro execution, or remote content.
- No direct stable publication, source-checkout substitute for installed proof, product fix in #492, incomplete-work disposition, Git-history rewrite, or mutation of ambient developer/host/project state.

## Capabilities

### New Capabilities

- `project-path-and-graph-integrity`: native identity, atomic legacy recovery, invalid-source admission, and complete bounded graph publication.
- `platform-parser-readiness`: exact Rust 1.98.0 authority, truthful optional-parser capability, warning-clean target selection, and clean macOS arm64 installed proof.
- `repository-content-intelligence`: built-in PHP, evidence-derived PHP guidance, and bounded exact-locator PDF/DOCX extraction.
- `repository-analysis-and-scale`: measured reverse callers/resources/baselines, typed entrypoint reachability, and deterministic bounded communities.
- `cli-installation-surface`: exact-native npm delivery, real host-reader consumption, and one complete collision-safe `atlas` surface.
- `repository-quality-evaluation`: narrow workflow timeout, responsibility-led test/module ownership, benchmark retention, and repeatable bounded evaluation.
- `release-candidate-delivery`: native release hierarchy, complete installed public-surface execution, holistic E2E, RC remediation, stable promotion, and closes-last acceptance.

### Modified Capabilities

None. Existing token-impact reporting, graph authority, database ownership, CLI/MCP schemas, and stable v0.4.5 behavior remain compatible unless an issue above explicitly owns the accepted additive correction.

## Impact

- Existing seven Rust crates, with concrete typed ownership rather than new architecture layers.
- Project-local SQLite keys, constraints, migrations, plans, transactions, WAL/concurrency, rollback/recovery, and authored/derived-state preservation where explicitly owned.
- Windows, Linux, macOS x64/arm64 runtime, installer, npm, plugin, generated host, CLI/MCP, parser, database, workflow, evaluation, and release boundaries.
- OpenSpec/IssueOps task mapping plus one temporary candidate release-graph promotion manifest, candidate-versus-published readiness, native GitHub hierarchy and dependency graph, affected-contract CI, one Dependabot campaign per release, version-matched guidance, issue-specific Mermaid documentation, and final hosted release state.
