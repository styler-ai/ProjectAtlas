# Progress Log

## Completed
- 2026-06-27: Added ProjectAtlas Codex plugin package and repo marketplace so other users can install `projectatlas` from the ProjectAtlas repository.
- 2026-06-27: Created `docs/projectatlas-3-architecture.md` for the Rust-native ProjectAtlas 3 direction: SQLite metadata, ProjectAtlas-native repository intelligence, progressive context funnel, health-check/lint, and token-savings telemetry.
- 2026-06-27: Added a strict Rust workspace vertical slice with core/db/fs/cli crates, SQLite storage, scanner, funnel commands, health-check, token telemetry, broad source extension support, and Rust lint/doc gates.
- 2026-06-27: Expanded the ProjectAtlas 3 goal in memory/spec: Rust-only final tree, complete ProjectAtlas-native repository intelligence, all-language support, large-codebase performance, and a seamless atlas-first agent workflow.
- 2026-06-27: Added Rust CLI repository-intelligence surfaces for fallback text search, exact line slices, settings inspection, watcher status, and SQLite purpose import during scan.
- 2026-06-27: Removed the legacy Python implementation/tooling from the ProjectAtlas tree and moved local verification to Cargo/Rust gates.
- 2026-06-27: Added tree-sitter-backed `projectatlas-symbols` with Rust/Cargo-first symbol graph extraction, relations, manifest/dependency symbols, broad fallback parsing, SQLite persistence, and exact symbol slices.
- 2026-06-27: Added native `projectatlas mcp` using the official `rmcp` Rust SDK. MCP tools use ProjectAtlas-native `atlas_*` names and return TOON text content inside the standard MCP JSON-RPC envelope.
- 2026-06-27: Added portable watcher refresh, legacy `.purpose` cleanup dry-run/apply command, plugin MCP config, runtime installer scripts, and E2E coverage for CLI, symbols, watcher, migration, and MCP tool routing.
- 2026-06-27: Inspected Code Index MCP source behavior for deep summaries. Its file summary is deterministic structured data from SQLite file rows and symbols; ProjectAtlas memory now treats that as the baseline to exceed, not a target to merely match.
- 2026-06-27: Added ProjectAtlas-native structured file summary surfaces through CLI `projectatlas summary` and MCP `atlas_file_summary`, backed by SQLite symbols/relations and enriched with purpose status, observed summary, imports/dependencies, calls, and token-conscious limits.
- 2026-06-27: Added purpose lifecycle tests around generated suggestions, agent approval, health review findings, and structured summary correctness for the Rust implementation.
- 2026-06-27: Architecture/source review subagent confirmed the remaining Code Index parity gaps: exports/docstrings/package metadata, deeper per-language semantic strategies, stronger cross-file call resolution, and event/debounce watcher semantics.
- 2026-06-27: Closed the immediate structured-summary metadata gap: schema v5 adds symbol export/documentation metadata, extractors detect Rust/TypeScript exports and Rust/Python docs, `summary`/`atlas_file_summary` expose package, docstring, exports, per-symbol exported flags, and per-symbol documentation, with SQLite/unit/E2E tests.
- 2026-06-27: Verified strict Rust gates and live ProjectAtlas self-checks after the metadata work: fmt, check, clippy `-D warnings`, workspace tests, doctests, rustdoc `-D warnings`, self-scan, summary, health-check, token, map, and lint all passed.
- 2026-06-27: Addressed final reviewer findings: indexed-file validation now protects CLI/MCP outline and slice tools from arbitrary local file reads, file summaries use a targeted DB path lookup, excluded directories are skipped during walking, legacy `.purpose` cleanup sorts once, and docs/adoption quickstarts include project root plus the `summary` gate.
- 2026-06-27: Final verification after reviewer fixes passed: fmt, check, clippy `-D warnings`, workspace tests, doctests, rustdoc `-D warnings`, self-scan, health-check, token report, map regeneration, and `projectatlas lint --strict-folders --report-untracked`.
- 2026-06-27: Refreshed Memory Bank for the next ProjectAtlas 3 loop: event-backed `notify` watcher first, TOON-in-MCP text content as the canonical agent payload, official/canonical crate preference, cross-platform path rules, and mandatory architecture/crate-choice subagent review after implementation.
- 2026-06-27: Implemented event-backed continuous watcher behavior with `notify`, debounce/exclude filtering, single-refresh mode for `watch --once`/MCP refresh, portable polling fallback, focused unit coverage, and an E2E test that edits a Rust file and verifies refreshed symbols.
- 2026-06-27: Addressed watcher/root review blockers: scans persist canonical project root in SQLite metadata, indexed reads/search/summaries/slices resolve from that root instead of cwd, folder filters normalize slash/backslash input, `watch-status` probes `notify`, and file watcher events perform partial DB/symbol refresh unless a full scan is required.
- 2026-06-27: Added `projectatlas-service` as a shared query/service crate for structured file summaries, moved summary DTOs and helper policy out of the CLI adapter, and added cross-file called-by summary data backed by targeted SQLite relation queries and E2E coverage.
- 2026-06-27: Addressed service-review blockers for structured summaries: `build_file_summary` now uses bounded section queries and exact SQLite counts, token baseline serialization lives in `projectatlas-service`, source fallback is explicit through `source_status`/`source_error`, and `called_by` avoids ambiguous same-name false positives through exact/path-aware matching plus regression coverage.
- 2026-06-27: Added `projectatlas mcp-config` for generated MCP configs with absolute runtime/DB paths, updated plugin installers to install from source or Git and generate `.projectatlas/projectatlas.mcp.json`, bumped the plugin manifest to 0.3.0, and stripped imported legacy `.purpose` files from the repo.
- 2026-06-27: Reworked release automation for marketplace readiness: `02-Release` now verifies the Rust workspace, builds Linux/macOS/Windows release archives, uploads them to a GitHub Release, and `03-Auto-Release` dispatches that workflow instead of creating an artifact-less release.
- 2026-06-27: Addressed final review blockers: incremental watcher symbol refresh now loads exact changed node paths, generated MCP configs include project config when present and installers default to the target cwd plus release-binary install before Cargo fallback, legacy `.purpose` stripping only touches scanner-indexed folders, `called_by` rejects duplicate module aliases through global alias counts, caller lookup limits per target with SQLite windowing, and CI/release lint stale maps before regenerating them.
- 2026-06-27: Refreshed Memory Bank after the latest ProjectAtlas 3 loop. Current verified state includes native MCP config generation with absolute DB/config paths, Windows plugin installer smoke success, release-archive workflow wiring, no legacy `.purpose` files on disk, and a clean self-scan with 63 files, 36 folders, 0 missing purposes, 1343 symbols, and 5889 relations.
- 2026-06-27: Hardened the repository-intelligence hot path after a Code Index parity/architecture review: `projectatlas-service` now owns bounded indexed search and exact line/symbol slicing, search uses `globset` repository globs with early-stop/scanned/truncated counters, symbol slices reject ambiguous duplicate names until parent/kind/line selectors are supplied, plugin installers skip stale non-ProjectAtlas-3 wrappers, and symbol extraction gained stronger Rust/TS/Python/Java/Go/C#/Kotlin/Zig/C/C++/Objective-C fixture coverage.
- 2026-06-27: Addressed the next parity-audit findings: scan/MCP scan/watch refresh/legacy cleanup now honor configured `[scan].exclude_dir_names`, indexed search supports native fuzzy matching through CLI and MCP, deep symbol builds expose worker and timeout controls backed by `rayon`, `overview()` uses SQL aggregates instead of whole-node materialization, and E2E tests cover configured excludes, fuzzy search, bounded search, identity-safe symbol slices, watcher filtering, and symbol build controls.
- 2026-06-27: Final verification after parity fixes passed: `cargo fmt --all --check`, `cargo check --workspace --all-targets --all-features --locked`, `cargo clippy --workspace --all-targets --all-features --locked -- -D warnings`, `cargo test --workspace --all-features --locked`, doctests, rustdoc `-D warnings`, ProjectAtlas scan/health/token/fuzzy-search/symbol-build/map/lint self-checks, plugin JSON checks, no legacy `.purpose` files, `git diff --check`, Windows PowerShell installer smoke, and Git Bash POSIX source-checkout installer validation. Native Linux/macOS release-binary installer validation remains a release/readiness follow-up.
- 2026-06-27: Fixed the final reviewer blocker: symbol builds now invalidate prior source-derived symbols, relations, and node summaries when a live file is skipped as too large, timed out, or non-UTF8, preventing stale symbols from surviving bounded refreshes. Added E2E regression coverage for `symbols build --max-bytes` and `watch --once --timeout-seconds 0`, and reran the full strict gate stack plus ProjectAtlas self-checks and Windows installer smoke.
- 2026-06-27: Added token telemetry hardening after architecture review: CLI usage tracking now measures the actual emitted payload for TOON vs JSON, MCP usage tracking remains TOON-inside-JSON-RPC, `token_overview()` aggregates in SQLite, `projectatlas token --view tui` renders an explicit human terminal dashboard, and tests cover core telemetry deltas, DB aggregation, CLI token/TUI flow, JSON serialization measurement, and MCP token reporting.
- 2026-06-27: Addressed final correctness blockers: scanner excludes now apply relative to the scan root and skip symlinked files; approved file purposes become stale on content-hash changes; health/lint surface stale/missing/suggested/duplicate-purpose issues from SQLite; `watch --once` preserves rich unchanged summaries; watcher refresh detects new files/folders and refreshes text/symbol indexes; broad search now uses persisted SQLite `file_texts`; and `projectatlas parity report` plus MCP `atlas_parity_report` provide a machine-checkable repository-intelligence readiness gate.
- 2026-06-27: Current ProjectAtlas self-check after the text-index/parity/stale-purpose loop reports 63 files, 36 folders, 0 missing/stale/suggested purposes, 99 approved purposes, 63 persisted SQLite text rows, 1561 symbols, 7493 relations, 0 health findings, passing repository-intelligence parity, a readable token TUI at 95.9% estimated savings, and DB-backed lint passing after map regeneration.
- 2026-06-27: Finished another Code Index MCP source sweep and closed a direct file-discovery parity gap: `projectatlas files --file-pattern <glob>` and MCP `atlas_files.file_pattern` now provide glob file discovery through shared `projectatlas-service`/`globset`; docs, skills, AGENTS template, and `projectatlas init` broad source-extension defaults were updated.
- 2026-06-27: Fixed a real MCP stdio runtime bug found by live JSON-RPC smoke testing: `projectatlas mcp` now enables Tokio timers for `rmcp`, and a new E2E subprocess test proves `tools/list`, `atlas_overview`, and `atlas_files.file_pattern` return TOON payloads through the MCP JSON-RPC envelope.
- 2026-06-27: Addressed final subagent review findings before push: generated TOON map rows now escape commas/quotes/newlines, regex/fuzzy SQLite text search streams rows and can stop early, repo-path validation is centralized in `projectatlas-core`, watcher status no longer reports active background watching from DB existence, `settings` reports cache/index/MCP/config/stat diagnostics, `reset-index`/`atlas_reset_index` provide dry-run/apply cleanup, and repository-intelligence parity validates actual MCP tool routes.
- 2026-06-27: Addressed final release-hardening review findings: `overview`/`folders`/`files` no longer materialize the full node table for agent navigation, SQLite builds unresolved health findings for health/settings/lint/parity, `PROJECTATLAS_NO_TELEMETRY=1` supports read-only review flows, Cargo manifest extraction now uses the official `toml` crate, config/manual atlas paths reject absolute paths and `..`, internal workspace deps are versioned, `deny.toml` makes `cargo deny check` a CI/release gate, and tests cover SQL ranking, SQL health, token-estimate visiting, no-telemetry behavior, TOML dependency variants, and unsafe metadata paths.
- 2026-06-27: Hardened `02-Release` against workflow-dispatch script injection by passing the requested tag through `RELEASE_VERSION`, validating it as `vMAJOR.MINOR.PATCH` before use, and removing direct `inputs.version` interpolation from shell and PowerShell scripts.
- 2026-06-27: Fixed `projectatlas map --force` release-drift behavior by preserving the existing `generated_at` value when generated file/folder hashes are unchanged; consecutive workspace-binary map runs now produce identical map file hashes.
- 2026-06-27: Fixed post-push GitHub workflow blockers: CI now queries live pull request metadata with `gh api` for milestone enforcement instead of relying on stale pull_request event payloads, and the docs Pages workflow moved `environment` under the deploy job.
- 2026-06-27: Fixed clean-checkout CI lint bootstrap: CI/release now run `projectatlas scan .` and repository-intelligence parity before DB-backed ProjectAtlas lint so the ignored SQLite index is created from committed atlas metadata.
- 2026-01-03: Added auto-detect language extensions for `projectatlas init`, updated docs/skills/tests, and removed dead config text from `detect_purpose_styles` (issue #111).
- 2026-01-03: Released v0.1.8 and published Highlights in the GitHub Release; merged post-release bump to v0.1.9.dev0 (issue #114).
- 2026-01-03: Clarified atlas overview counts (source/nonsource/total) and documented the nonsource tracking model in docs/skills (issue #118).
- 2026-01-02: Added ProjectAtlas Memory Bank files under `.codex/rules/memory` and tracked them in the non-source list.
- 2026-01-02: Switched auto-release to `--generate-notes` and enforced issue milestones in PR checks (CI).
- 2026-01-02: Released v0.1.4 with updated release notes linking PRs/issues.
- 2026-01-03: Added the legacy one-command local verification wrapper, later removed during the ProjectAtlas 3 Rust-only migration.
- 2026-01-03: Added pre-push hook to run the full check suite before pushes and documented it.
- 2026-01-03: Released v0.1.5 (milestone closed, tag published, dev bumped to v0.1.6.dev0).
- 2026-01-03: Aligned config template and configuration docs with the default asset extensions list (fonts included); full check suite passed.
- 2026-01-03: Released v0.1.7 with language-agnostic Purpose header support (line/block comment styles), expanded default language coverage, updated docs/skills/config, and added unit tests (issue #101).
- 2026-01-03: Required Highlights in release notes via GitHub rules updates (issue #108).

## Pending Work
- Close the remaining deep-summary parity gaps: richer language-specific signatures/import-export normalization, package/namespace fidelity by language, and parser-specific cross-file import/call resolution quality.
- Expand language-specialized extraction beyond generic tree-sitter traversal for Rust, Python, JavaScript/TypeScript/TSX, Java, Kotlin, C#, Go, Objective-C, Zig, C/C++, and broad fallback families.
- Architecture hardening follow-ups: split the MCP adapter or add a shared application layer as `main.rs` grows, consider SQLite FTS5 for measured full-text search needs, and consider node-id-backed symbol graph storage for rename resilience.
- Run another architecture-review subagent and correctness review only if material changes land after the current final gate/push loop.
- Complete marketplace publication hardening: publish/verify the first release archives at their final URLs, validate POSIX installer behavior on Linux/macOS, and finalize marketplace README/install path.
- Final impartial end-to-end tester, DRY/principles reviewer, and architecture reviewer subagents have run for this loop; rerun only if more material changes land.
- Keep the MCP subprocess E2E smoke in the final gate stack because in-process MCP tests alone do not cover runtime-builder mistakes.
- Expand performance testing for very large repositories after the full functional gate stack is green, especially summary latency over files with many symbols and large relation tables.
- Keep legacy migration explicit for external adopters: import `.purpose` files and Purpose headers into SQLite, keep TOON as optional export, and strip legacy metadata only through dry-run/apply commands.

